<!--
  CANONICAL LOCATION: tasks/memory/MEMORY.md (in the project, version-controlled).
  The harness does not auto-inject this; CLAUDE.md directs reading it at session
  start.  Add new durable notes as a file here + a one-line index entry below.
  Never write to ~/.claude/.

  Entries are grouped by *the moment the rule fires*.  HARD rules are bolded.
  Intentional cross-listings exist (cost: one line; benefit: rule visible at its
  trigger).  Index lines are deliberately TERSE (token-efficiency, 2026-06-06):
  trigger + imperative only — the why/history lives in each rule file; read it
  before relying on nuance.
-->

## 0. ABSOLUTE BANS — read before EVERY find/ls/grep/glob

- **[NEVER traverse outside the workspace root](feedback_no_home_search.md) — ABSOLUTE. Root per-host: `/Users/ravn/z80/` (macbook), `/home/ravn/z80/` (sonnyboy). No `find /`~`ls ~`/`mdfind`/`locate`. Check path before every find/ls/glob.**

## 1. Always-on (every response)

- **[Check memory BEFORE coding](feedback_check_memory_before_coding.md) — HARD: at task start (not just session start) scan MEMORY.md for applicable sections, READ the linked files, NAME the rules that apply in your first response, THEN start coding. Session-start read is necessary but not sufficient.**
- **[Revalidate historical compiler claims](feedback_revalidate_historical_compiler_claims.md) — HARD: before acting on any historical compiler-perf claim (size/speed/miscompile/"pass X pessimizes"), re-run the original measurement on a clean rebuild. Stale-rebuild + backend movement invalidate old numbers; current measurement wins. CASCADING: any replacement code (heuristic/fix) you write needs the same scrutiny — use a no-op control cell, not the heuristic's own output, to validate it.**
- **[Rebuild ALL Z80 tools after backend edit](feedback_rebuild_all_z80_tools.md) — HARD: after editing llvm/lib/Target/Z80/, `ninja -C build-macos clang llc lld` (all three; LLVMZ80CodeGen is in all). LTO/`-flto`/PROM builds go through `ld.lld` — rebuilding only clang+llc leaves it stale → phantom "LTO differs" bugs (2026-07-08 incident).**
- **[No-op control measurement](feedback_no_op_control_measurement.md) — HARD: for any new heuristic / cost-model hook / pass override, run THREE cells (baseline / no-op control / feature-ON) and check that no-op-control matches baseline byte-identically. If not, the new code has a presence-cost side effect; investigate before trusting feature-ON measurements.**
- **[Token-efficiency disciplines](feedback_token_efficiency.md) — HARD: (1) never raw logs into context — file + grep/tail summary, failing slice only; (2) long builds/runs in background; (3) handoff file + suggest fresh session at work-item boundaries.**
- **[Communication Style](feedback_style.md) — think out loud, concise, no apologies, record prompts**
- [Suggest model switch when warranted](feedback_suggest_model_switch.md) — flag Opus vs Sonnet fit before starting a task; one sentence suffices
- **[NEVER apologize](feedback_no_apology.md) — HARD: no "sorry"/self-criticism; report state + next action**
- **[No compliments](feedback_no_compliments.md) — HARD: start with the substantive answer**
- **[No aphoristic flourishes](feedback_no_aphoristic_flourishes.md) — HARD: never wrap a decision in a maxim**
- **[Fix as close to the source as possible](reference_quad_init_backend_split.md) — HARD (user directive 2026-08-05: "Altid fixe ting så tæt på kilden som muligt"): fix in the earliest/owning layer (frontend→backend/MC→bridge→assembler), never a downstream post-processing band-aid. Ask "who emits/produces this, and can that producer simply not?" before reaching for a workaround. See tasks/lessons.md 2026-08-05 (ravn/z88dk#27: backend one-liner replaced an external perl pre-pass).**
- **[Session-break phrasing](feedback_session_break_phrasing.md) — HARD: when a checkpoint warrants a fresh session, state exactly "This is a good place to start a new session." — no preamble, no reference to hour/length/fatigue/"fresh head". Otherwise say nothing.**
- **[Show thinking — TIERED](feedback_show_thinking.md) — HARD: full narration at decisions/diagnoses/forks/surprises; one-liners during mechanical loops (tiered 2026-06-06)**
- **[Dig one level deeper before parking](feedback_dig_deeper_before_parking.md) — HARD: before declaring "deferred/multi-week", instrument + bisect 30 min first**
- **[Minimal repro BEFORE source dive](feedback_minimal_repro_before_source_dive.md) — HARD: for "why does X fail under tool Y" questions, do a 30-second minimal repro (failing form + variants that the assumed root cause says shouldn't fail) BEFORE reading the implementation source. Don't post "suggested fix" lines in filed issues without proving them via repro.**
- **[Zoom out on recurring pattern](feedback_zoom_out_on_recurring_pattern.md) — HARD: after 2-3 fixes of one class, STOP and find the systemic cause unprompted**
- **[Audit the oracle, not just the fix](feedback_audit_oracle_not_just_fix.md) — HARD: bug found by luck → build the detector that would have caught it on purpose**
- **[Verify process state by full enumeration](feedback_verify_process_state_full_enumeration.md) — HARD: never claim "clean" from a `ps | grep` of expected names; enumerate fully + reconcile**
- [No Unicode arrows](feedback_no_unicode_arrows.md) — ASCII `->`, not `→`
- **[NEVER unquoted "===" in shell](feedback_no_double_equals.md) — HARD: zsh silently truncates the command; use `---` as separator**
- **[State certainty](feedback_state_certainty.md) — HARD: fact only if verified this session; surface ALL doubt; applies to issues/commits too**
- [Ask about design decisions](feedback_ask_about_design_decisions.md) — at non-obvious forks, lay options out, user picks
- [No ask in debug loop](feedback_no_ask_in_debug_loop.md) — inside standing-auth debug cycle, run the next step
- **[Probe must not consume the resource](feedback_probe_must_not_consume_resource.md) — a health-check that grabs a single-use connection/lock/token corrupts what it verifies; if a probe passes but the guarded thing still fails identically, run the probe TWICE on a fresh resource — 2nd fail = destructive probe (cpnos #119: ping wedged single-connection MP/M master)**

## 2. Before any commit / PR / issue

- **[Never create UNSOLICITED PRs](feedback_no_pull_requests.md) — HARD: no `gh pr create` unless asked this turn; never per-bug/fix PRs upstream**
- **[Auto-close needs a keyword in the SAME repo](reference_quad_init_backend_split.md) — a commit only auto-closes an issue when the message has `Fixes/Closes/Resolves #N` AND lands on the default branch of the SAME repo. A bare mention (`(#N)`, `owner/repo#N`) never closes; a cross-repo reference (e.g. ravn/llvm-z80 commit mentioning `ravn/z88dk#27`) never closes the other repo's issue. `gh issue view N` in the local z88dk clone resolves to UPSTREAM z88dk/z88dk — use `--repo ravn/z88dk` for the fork. (ravn/z88dk#27 had to be closed manually 2026-08-05.)**
- **[Explain before filing](feedback_explain_before_filing.md) — HARD: root cause explained in chat + explicit per-filing "go ahead" before ANY upstream post; check target tracker for duplicates FIRST. Draft approval ≠ filing approval. 2026-07-08: filed mamedev/mame#15664 without gate — closed immediately.**
- **[Self-caused bug? reflect on instructions](feedback_self_caused_bug_reflect_on_instructions.md) — HARD: when filing/finding a bug that traces to AI-authored code (check git trailers for `Co-Authored-By: Claude*`), pause and identify the instruction (test-coverage rule OR code-convention) that would have prevented it. Offer to save as memory BEFORE fixing.**
- **[Peephole adjacency uses next_nodbg](feedback_peephole_next_nodbg.md) — HARD: any peephole walking adjacent MIs uses `MachineBasicBlock::next_nodbg()` / `skipDebugInstructionsForward()`, NEVER raw `std::next` (DBG_VALUE pseudos break adjacency match under `-g`)**
- **[Peephole lit tests must exercise -g](feedback_peephole_test_with_g.md) — HARD: every peephole lit test has TWO RUN lines (-O2 and -O2 -g) with identical FileCheck pattern; catches `-g`-defeats-peephole class silently**
- **[File bugs, not fixes](feedback_file_bugs_not_fixes.md) — HARD: upstream filings are BUGS (repro, current vs expected, root cause, evidence, NO fix); maintainer decides how to fix; user must understand each well enough to defend**
- **[Thorough tests for upstream bugs](feedback_thorough_tests_for_upstream_bugs.md) — HARD: upstream submissions need matrix-grade test cases (lit + runtime, negatives + positives + controls, self-checking expecteds), not just a minimal repro**
- **[Cross-machine workflow](feedback_cross_machine_workflow.md) — HARD: commit-push at end of every working segment; pull-with-submodules at start; live narrative in `tasks/handoff/`**
- **[ravn/llvm-z80 Actions OFF](feedback_ravn_llvm_z80_ci_disabled.md) — HARD: never expect CI on ravn/llvm-z80; local oracle is the merge gate; don't re-enable without user direction**
- **[No commit on lit+size alone](feedback_no_commit_first_version.md) — HARD: combiner/ISel/lowering changes need value oracle (test-runner + MAME) before commit**
- **[Consult rules before acting](feedback_consult_rules_before_acting.md) — HARD: search MEMORY.md before any fix; commit message includes `Rules-checked:`**
- **[Grep repo docs before deriving](feedback_grep_repo_docs_before_deriving.md) — HARD: grep for existing `*_REFERENCE.md` before re-deriving encodings**
- [No UNSOLICITED Upstream Issues](feedback_no_upstream_issues.md) — default: file in ravn/* forks; curated submissions only on user direction
- **[Upstream routing](feedback_upstream_routing_two_targets.md) — HARD: generic-LLVM bugs → llvm/llvm-project (or local XFAIL); llvm-z80/llvm-z80 ONLY for Z80-specific**
- **[MAME upstream routing](feedback_mame_upstream_routing.md) — HARD: never file in any MAME repo without explicit per-issue permission; devices → mamedev, rc702 driver → ravn/mame**
- **[No local zsdcc fixes](feedback_no_local_zsdcc_fixes.md) — HARD: root-cause + repro + `wontfix` + report upstream; clang fixes still local**
- **[No upstream sdcccall 0/1 discrepancies](feedback_no_upstream_sdcccall_discrepancies.md) — HARD: sdcccall 0/1 ABI mismatches are known build-config issues (z88dk warning 296), NOT upstream-fileable; work around locally (SKIP_CELL or register-arg shims)**
- [File dep bugs in ravn/* forks](feedback_file_issues_in_forks.md) — with repro + test case
- [Always test compiler bugs](feedback_compiler_bug_test.md) — XFAIL lit test for every clang Z80 codegen bug
- [Attribution line on filed issues](feedback_issue_attribution_line.md) — append `--- / _Filed by GitHub Copilot on behalf of @ravn._` to every issue body
- [Comment on issue when fix committed](feedback_issue_comment_on_fix.md) — AUTO (no prompt): after fix commit, post comment with hash + what changed + verification + remaining notes
- [Test before fix](feedback_test_before_fix.md) — failing test before implementing
- [Plan thoroughly first](feedback_plan_thoroughly_first.md) — explicit step-by-step plan + confirm before any non-trivial work
- [Production-hard, AES/upstream-soft](feedback_production_hard_aes_soft.md) — production triplet is the load-bearing workload; AES + upstream-correctness are additional signal
- [BSD awk only on macOS](feedback_bsd_awk_only_on_macos.md) — avoid strtonum/gensub/asort; use Python for hex parsing
- [Project timeline log](feedback_timeline_record_keeping.md) — append to rc700-gensmedet/tasks/timeline.md per meaningful change

## 3. Before any memory-layout / linker / address change

- **[RC702 IVT page constraint](project_rc702_ivt_page_constraint.md) — IM 2 IVT page must not overlap display 0xF800..0xFFCF; valid: 0xEC00/0xED00 (BSS) or 0xF500 (resident)**
- [rcbios 32-bit RTC = diffs only](reference_rcbios_rtc_counter_diffs_only.md) — rtc0/rtc2 (0xFFFC/0xFFFE) is a 50Hz boot-relative tick counter; wraps ~2.72yr; use for elapsed-time differences, NEVER as a since-epoch timestamp. Same 50Hz as z88dk rc700 clock().
- **[RC702 bank2h PROM mirror](feedback_rc702_bank2h_mirror.md) — HARD: 0x2800..0x2FFF is PROM1-mirror, NOT RAM**
- **[Grep mem_map before BSS literal](feedback_grep_memmap_before_bss.md) — HARD: grep emulator mem_map before allocating BSS at a literal address**
- **[Slave RAM state outside TPA](feedback_slave_state_outside_tpa.md) — HARD: pin slave state to SNIOS reserved area (0xED00..0xF7FF), never inside TPA**
- **[Phase-boundary state-address audit](feedback_state_address_phase_audit.md) — HARD: re-audit state addresses when lifecycle changes (prom-init → RAM-resident)**
- **[Audit memory layout on port](feedback_memory_layout_on_port.md) — HARD: audit layout invariants proactively when porting; prefer HIGH(symbol) over literals**
- **[No literal memory addresses](feedback_no_literal_addresses.md) — HARD: linker-derived or `.sym`-extracted only; literals OK for ports/vectors/magic**
- **[Cross-stage --defsym atomic](feedback_relink_dependencies_atomically.md) — HARD: C decl + linker script + Makefile awk + defsym in same commit; mind underscore count**
- [cpnos.com address coupling brittle](project_cpnos_address_coupling_brittle.md) — never replicate hand-typed cross-image addresses
- [rcbios + cpnos code sharing (future)](project_cpnos_rcbios_code_sharing.md) — VRTC ISR / DMA / 8275 / PIO+SIO duplicated across both; factor after INIR work settles (#22)
- **[Ring-shrink + INIR coupling](feedback_ring_shrink_inir_coupled.md) — HARD: cpnos PIO-B `pio_rx_buf` 256→16 B sizing ASSUMES INIR drains data bytes direct to msg+5; without INIR a 41-byte data block overflows 16 B and slave deadlocks at netboot. Don't ship the shrink without INIR; don't bisect by forcing INIR off without restoring the ring.**
- **[Verify HW register is load-bearing](feedback_verify_hw_register_load_bearing.md) — HARD: when modifying init-time hw reg writes OR the ISR/code consuming them, verify the bit is actually load-bearing (consumer behaves differently without it); vestigial config accumulates and masks bugs**
- **[Bundle layout migrations proactively](feedback_bundle_layout_migrations_proactively.md) — HARD: when deferring a layout move with "we don't need it yet", check region headroom; if <200 B, the next workstream will need it — bundle now**
- [Sentinel preconditions](feedback_sentinel_preconditions.md) — re-derive "real data ≠ sentinel" at every use site

## 4. Before any build / compile / link flag change

- **[+static-stack only for non-recursive code](feedback_static_stack_nonrecursive_only.md) — HARD: `+static-stack` is non-reentrant and SILENTLY miscompiles recursion (no error, wrong answer). Never enable globally; establish non-recursion first. Evidence: nqueens wrong + 404K vs 53M ts under `clangp`.**
- **[Check sibling subprojects](feedback_check_sibling_subprojects.md) — HARD: grep siblings for the same flag, mirror their wrapping**
- **[Symmetric recipes per compiler](feedback_symmetric_recipes_per_compiler.md) — HARD: parallel `ifeq COMPILER` recipes must emit the SAME artifact set**
- **[Build-var artifacts content-check, not mtime](feedback_build_var_artifacts_content_check.md) — HARD: files embedding TRANSPORT/COMPILER regen via content grep**
- [Test both compilers](feedback_dual_compiler_test.md) — rcbios changes build with BOTH z88dk and clang before commit
- **[llvmz80 runtime-test gotchas](feedback_llvmz80_runtime_test_gotchas.md) — use `-Cg-O2` (bare `-O2` leaves clang at -O0 → -O2-only codegen like `.rodata.cstN` never emitted → vacuous pass); verify const data in the SHELL (C-side `x[i]==LIT` folds from the const initializer). Build against classic, not newlib.**
- [Check memory for builds](feedback_check_memory_for_builds.md) — check memory for correct build flags first
- **[Use --math32 for llvmz80 float builds](feedback_use_math32_flag.md) — use the literal `--math32` flag (don't substitute `-lmath32`/`-lm`/hand-rolled `-L`). As of ravn/z88dk#44 (2026-08-09) zcc auto-links the fmath bridge + auto-injects `-mllvm -z80-float-sdcccall0`, so plain `--math32` is a COMPLETE FP runtime (bridge rebuilt, full sf family: add/sub/mul/div, cmp/gt/ge/unord, fixsfsi/floatsisf). #44 bridge blocker CLEARED — softfloat role retireable.**
- [Build-tool binaries](reference_build_binaries.md) — cmake/ninja from CLion bundle (mac); native llc/clang in llvm-z80/build-macos/bin
- **[Record macOS utility surprises](feedback_record_macos_utility_surprises.md) — when a BSD utility misbehaves vs GNU, SAVE a memory note + workaround (no brew here; python3 is the fallback).**
- [macOS awk lacks strtonum](reference_macos_awk_no_strtonum.md) — default awk is BWK not gawk; no strtonum/gensub/hex-parse. Use python3/printf/`$((16#..))` for hex crunching.
- [Z80 tool paths](reference_z80_tool_paths.md) — full paths + canonical invocations, BUILD_DIR/PATH overrides
- **[AVR density oracle](feedback_avr_density_oracle.md) — HARD: before blaming a generic pass or filing upstream, compile the repro for in-tree AVR; AVR-cheap + Z80-expensive = OUR backend gap (and AVR shows the mechanism)**
- **[Don't kill ninja mid-build](feedback_dont_kill_ninja.md) — HARD: SIGKILL truncates .ninja_log → 1700+ step rebuild; Ctrl-C ONCE. Separate build DIRS OK concurrently. Log to scratch/ninja-*.log; no pgrep|kill probes.**
- **[Read build-tool docs before improvising](feedback_read_tool_docs_before_improvising.md) — rm only .ninja_log NOT .ninja_deps (deps DB → full rebuild); `set -o pipefail` for tee'd ninja (else exit code is echo's, not ninja's).**
- **[Ninja clang+llc together](feedback_ninja_clang_llc_together.md) — HARD: after backend change, `ninja clang llc` BOTH**
- [Docker for missing binaries](feedback_docker_binaries.md) — don't suggest installing
- **[Docker shim batch](feedback_docker_shim_batch.md) — HARD: build Makefiles batch multi-step Docker calls into ONE `docker run sh -c "..."`; ~150-500 ms container startup tax dominates otherwise**
- [Docker invocation budget](feedback_docker_invocation_budget.md) — all hosts wherever Docker is used; track per-workflow call count; flag when any routine pipeline exceeds ~100
- [Build zmac if missing](feedback_build_zmac.md) — `make` in zmac subfolder
- **[zmac local labels are global](feedback_zmac_local_label_scope.md) — HARD: dotted locals collide across subroutines; prefix with initials**
- [macOS timeout](reference_macos_timeout.md) — no GNU timeout; use Bash timeout param or perl alarm
- [C23 subset](feedback_c11_standard.md) — tested C23 features only (true/false/nullptr/typeof/0b)
- [No Undocumented Default](feedback_no_undocumented_default.md) — no undocumented Z80 instructions without +undocumented
- [Check for Undocumented](feedback_undoc_check.md) — grep asm for IXH/IXL/IYH/IYL after compiling
- [Always regenerate timestamp](feedback_timestamp.md) — delete builddate.h before every BIOS/PROM build
- [MAME OSD=sdl](feedback_mame_osd_sdl.md) — not sdl3; full command in docs/MAME_RC702.md

- **[Verify CMake fixes with compile_commands](feedback_verify_cmake_fixes.md) — HARD: a CMake/.clangd include fix is not done until you `cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON` and confirm the failing file's actual -I flags; compiler-resolves-header is NOT proof. Prefer absolute `${CMAKE_CURRENT_SOURCE_DIR}` in CMakeLists over relative -I in .clangd (clangd resolves those from the source dir).**

## 5. Before any llvm-z80 compiler-codegen change

- **CRITICAL framing — [Z80 backend unfinished](project_z80_backend_unfinished.md): goal is to FINISH the backend correctly, not optimize a finished one**
- **CRITICAL framing — [Z80 staged collaboration model](project_z80_upstream_goal.md): near-term llvm-z80/llvm-z80; long-term llvm/llvm-project; collaborate with owner**
- **[No commit on lit+size alone](feedback_no_commit_first_version.md) — HARD (cross-listed §2): value oracle required**
- **[+static-stack only for non-recursive code](feedback_static_stack_nonrecursive_only.md) — HARD (cross-listed §4): non-reentrant, silently miscompiles recursion**
- [Z80AutoStaticStack cross-TU soundness fix](autostaticstack_cross_tu_soundness_2026-08-11.md) — auto-inject pass now gated `hasLocalLinkage() || !ReachesExternal`; fixed cross-TU mutual-recursion miscompile (test_09). Production explicit-`+static-stack` byte-identical (verified). Explicit opt-in still bypasses the gate by design.
- [Always test compiler bugs](feedback_compiler_bug_test.md) — XFAIL lit test per bug
- [Verify codegen not just size](feedback_verify_codegen.md) — read disassembly per compiler; same size ≠ same behavior
- **[Compiler is not trusted](feedback_compiler_not_trusted.md) — HARD: inspect generated asm BEFORE blaming source/runtime/hardware**
- **[Verdict AFTER real pass output](feedback_verdict_after_real_pass_output.md) — HARD: show the IR/asm the named pass actually produces + contamination + remaining doubt BEFORE stating any verdict; synthetic worst-case ≠ evidence**
- **[Fix at the layer that owns the emission](reference_quad_init_backend_split.md) — on a dialect/emission mismatch ask "who emits this token, and can that emitter simply NOT?"; fix in the earliest suitable layer (frontend→backend/MC→bridge→assembler) before adding a text pre/post-pass. ravn/z88dk#27: backend `Data64bitsDirective=null` replaced an external splitquad.pl perl pass (see also tasks/lessons.md 2026-08-05).**
- **[double is float32 on z80; llvmz80-softfloat/ RETIRED](project_double_is_float32_retire_softfloat.md) — since #277 (2026-08-05) `double`==`long double`==32-bit binary32 (`sizeof==4`, `sf` libcalls, math32 runtime). The 64-bit `llvmz80-softfloat/` closure was superseded and is now RETIRED/deleted (ravn/z88dk#44, DONE 2026-08-09); `LLVMZ80RTLIB` wiring removed from zcc. FP runtime = auto-linked `llvmz80_fmath.lib` bridge via `--math32`. The `.rodata.cstN` guard (#30) lives in-tree ABI-independently as `z88dk/test/clang/runtime_rodata_cstn`.**
- [Late-opt audit](reference_late_opt_audit.md) — session-37 Keep/Migrate/Delete classification of all 46 peepholes
- [Root-cause over peephole](feedback_root_cause_over_peephole.md) — favor upstream fixes over post-RA peepholes
- **[Peephole safety guards](feedback_peephole_safety_guards.md) — HARD: erase/move/convert peepholes need complete liveness + slot-aliasing + iterator guards**
- [Proper fixes — backend immature](feedback_proper_fixes_immature_backend.md) — question prior design decisions, including my own past code
- [T-states Matter](feedback_tstates.md) — evaluate size AND execution time
- [Don't fight SDCC iCode](feedback_dont_fight_sdcc_icode.md) — no `static`-locals to preempt SDCC; clang uses `+static-stack`
- **[SDCC block-scope extern broken](feedback_sdcc_block_scope_extern.md) — HARD: cross-TU externs at FILE scope under z88dk-SDCC**
- [Prefer C over inline asm](feedback_prefer_c_over_asm.md) — file codegen issues instead of ~6-10 B asm rewrites
- **[Z80 copies have spurious mayLoad/mayStore](feedback_z80_copy_spurious_mem_flags.md) — HARD: use `!MI.memoperands_empty()`, not mayLoad()/mayStore()**
- **[zeroext is ABI, not source-narrow](feedback_zeroext_is_abi_not_source.md) — HARD: use computeKnownBits before narrowing**
- **[TruncInstCombine: swap before probe](feedback_truncinstcombine_swap_before_probe.md) — HARD: modify IR users BEFORE `getBestTruncatedType`; rollback on failure branch**
- [IX caller-saved after #12](project_ix_caller_saved_after_12.md) — IX allocatable only pays caller-saved; revisit after #12
- [pi CSE / branch-fold miscompile PARKED](project_pi_cse_branchfold_parked.md) — Branch Folder unsound hoist (exposed by MachineCSE); production unaffected; don't re-investigate, don't flip `-z80-enable-cse` default ON until upstream fix
- [Fork-local pass naming = upstream candidacy honesty](feedback_fork_local_pass_naming.md) — `Z80*` prefix is locative not semantic; if a pass's body is target-agnostic, name with the operation (`*Recognize`/`*Combine`) AND record the upstream debt in `upstream-coherence-map`. `Z80LoopIdiomFill`→`Z80PatternFillRecognize` (2026-06-09) is the pinned example.
- [#212-class HL borrow-save audit pattern](project_212_class_borrow_save_pattern.md) — `if (HLLive) PUSH_HL` without IMPLICIT_DEF for dead half trips verifier when fastregalloc places undef; 5+ latent sites in Z80InstrInfo.cpp filed as #239; reflex check whenever auditing pseudo-expanders that borrow HL/IX/IY
- **[z88dk classic calling conventions under clang](reference_z88dk_calling_conventions.md) — `__smallc`=sdcccall(0) WORKS; `__z88dk_callee` + `__z88dk_fastcall` are no-ops (fastcall only 16-bit-safe: z88dk L/HL/DEHL vs clang A/HL/HLDE). clang C ABI: args HL/DE, 16-bit RETURN in DE, no callee-saved GPRs. Helper name mismatch = backend emits libgcc names, `l/clang/` shipped SDCC names (whole div/mod/mul family dead-named). Native z88dk-CC support is FEASIBLE (proven `Z80_SDCCCall0`/`Z80_AllReg` pattern); plan in `llvm-z80/tasks/plan-2026-07-10-z88dk-calling-conventions.md`**

## 6. Before any MAME / boot / test run

- **[rc759 CCP/M boots ~290 emulated s](reference_rc759_mame_c_verification.md) — HARD: turnkey `menu.cmd`/autostart program does NOT run until ~t175-290. ALWAYS `-seconds_to_run 400` + read a LATE snapshot frame. An early `*** PICCOLINE TEST V.2.1 ***` is the ROM monitor MID-BOOT, NOT a crash. (Mis-read 2026-08-26 as "compact UNZIP crashes"; real result was a clean inflate PASS once boot finished.) `rm -f nvram/rc759/nvram` before each run to force autoboot; regnecentralend MUST have FDC fix 59b21dc1312 or CMDs won't load (spurious WD2797 LOST DATA).**
- **[emu2 default TPA is ROOMIER than real MAME](reference_emu2_tpa_pool_fidelity.md) — HARD: emu2 is a differential oracle, not truth. Its default `-m 210` gives more runtime memory than the rc759 XIOS's fixed 384 KB / ~293 KB TPA, so a program that passes under default emu2 can OOM/diverge on real MAME. Use `emu2 -m 190` to match rc759 byte-for-byte before trusting a CP/M-86 memory result.**
- **[Verify banner timestamp before trust](feedback_check_banner_timestamp.md) — HARD: banner timestamp vs BUILD_INFO_STR before any diagnosis**
- **[Polypascal stage-1/2 flake = MP/M daemon state](feedback_polypascal_stage1_flake.md) — first try `make _kill-mpm; sleep 5-8; retry`. ★ BOTH PIO+SIO failing identically (transport-agnostic), or after you ran standalone `mpm`/`nc`-probed :4002 = master state YOU contaminated; kill your own stragglers (ps/lsof :4002), NOT codegen.**
- **[cpnos PIO netboot: NO -autoboot_script](feedback_cpnos_pio_netboot_no_autoboot.md) — HARD: any autoboot (even empty) breaks the wall-clock-coupled PIO cpnet_bridge netboot (stalls at 1st LOGIN byte); drive cpnos via host-side SIO-B injector (`cpnos_polypascal_inject.py`), `-nothrottle`, `wait_mpm_ready.py` gate. Never restore local/mpm-net2-1.dsk from library (stale SERVER.RSP → gettod `ff`); rebuild MPM.SYS.**
- **[mpm-net2 weirdness — first fix is stop+rebuild+restart](feedback_mpm_server_first_fix.md) — HARD: ANY unexpected mp/m server behavior → stop, rebuild, restart, retry BEFORE theorizing. If still bad: ASK the user.**
- **[Session-start: kill daemons BEFORE first test](feedback_session_start_kill_daemons.md) — HARD: `make -C cpnos-in-c _kill-mpm; sleep 8` before first run + between COMPILER switches**
- **[Screenshot to verify](feedback_screenshot_to_verify.md) — HARD: capture a MAME screenshot to verify boot; PASS lines + memory dumps are NOT enough**
- **[Visual capture for display path changes](feedback_visual_capture_for_display.md) — HARD: CRT ISR / DMA refresh / 8275 / display BSS changes need multi-frame video verification; textual PASS doesn't catch garble**
- **[Black screen is fatal](feedback_black_screen_fatal.md) — HARD: halts all other investigation**
- [Black screen → CRT ISR not firing](feedback_black_screen_crt_isr.md) — suspect EI / IVT slot 2 / CTC ch2 in that order
- [MAME Banner Check](feedback_mame_banner.md) — verify compiler (CL=clang, ROA375=SDCC)
- [MAME PROM Checksum](feedback_mame_checksum.md) — verify CRC matches built PROM
- [MAME ROM warning is a bug](feedback_mame_rom_warning.md) — fix BAD_DUMP, don't dismiss
- [Full rebuild before MAME](feedback_mame_rebuild.md) — rm .o + full rebuild first
- **[MAME always windowed + timeout](feedback_mame_always_window_timeout.md) — HARD: every launch needs `-window` AND finite `-seconds_to_run N`; never fullscreen, never timeout 0. User can't stop a runaway fullscreen instance.**
- **[Never concurrent builds, never kill a build](feedback_never_concurrent_or_kill_builds.md) — HARD: one ninja/make at a time (shared obj dir races); never pkill a build (deletes partial .o -> full rebuild). Wait for it to finish/fail.**
- [Fresh BIOS+PROM before MAME](feedback_mame_fresh_build.md) — rebuild both
- [Run MAME at full speed](feedback_mame_full_speed.md) — `-nothrottle` in unattended tests
- **[Disable MAME audio in ALL tests](feedback_disable_audio_in_tests.md) — HARD: pass `-sound none`.  Motor sounds annoy user; CoreAudio causes SIGPIPE (exit 141) under sustained background-test load.**
- **[MAME windowed only](feedback_mame_windowed_only.md) — HARD: always `-window`, never fullscreen**
- **[Headless host = no MAME window](feedback_host_no_graphics.md) — HARD: on sonnyboy use `SDL_VIDEODRIVER=dummy` (or xvfb-run); snapshots/AVI still work**
- [MAME interactive timeout](feedback_mame_interactive_timeout.md) — ~30s Bash timeout suffices
- [Lua no port reads](feedback_lua_no_port_reads.md) — use install_read_tap; double reads break devices
- [Lua retain tap handles](feedback_lua_retain_tap_handles.md) — store install_*_tap return in a long-lived table or GC frees it -> segfault in lua_topointer
- **[Lua errors are fatal](feedback_lua_errors_fatal.md) — HARD: any `[LUA ERROR]` invalidates the run; fix lua, re-run, THEN interpret**
- **[Display address from DMA, never hardcode](feedback_display_addr_from_dma.md) — HARD: read base from Am9517A DMA ch2 (autoload 0x7A00, roa375 0x7800)**
- [Bench self-termination](feedback_bench_must_self_terminate.md) — taps call `manager.machine:exit()` on finish
- [No permission for MAME/MP/M launch](feedback_mame_mpm_no_permission.md) — standing authorization to spawn/kill
- [Pre-launch change summary](feedback_show_changes_before_launch.md) — list edits/build/PROM/daemons + hypothesis before every run
- [MP/M shutdown via BYE](feedback_mpm_bye_shutdown.md) — `BYE` on console, not kill
- [CP/NOS MAME prereqs](project_cpnos_mame_prereqs.md) — MP/M up + no stale conn + NDOS in-sync + cpnos built
- [Check port 4002 before MAME](feedback_port4002_check.md) — abort if occupied
- [Watch slow commands](feedback_watch_slow_commands.md) — unusually slow ops flagged; prior symptom of register clobbering
- [MAME keyboard test](reference_mame_keyboard_verification.md) — natkeyboard:post() chain verification
- [pio-irq-fix test topology](project_pio_irq_test_topology.md) — `make pio-irq-netboot` (PIO-B → mpm-net2 :4002)

## 7. Before file/script ops

- **[NEVER traverse outside the workspace root](feedback_no_home_search.md) — see §0; ABSOLUTE**
- **[cpmtools live at `~/.local/bin`](reference_rc759_mame_c_verification.md) — `mkfs.cpm`/`cpmcp`/`cpmls`/`cpmrm`/`floptool` — INVOKE BY FULL PATH (`$HOME/.local/bin/...`); this is the one sanctioned home path, but NEVER `find`/`ls` inside home to locate them (§0). They read the disk geometry from a `./diskdefs` in the CURRENT DIR — the `DISKDEFS` env var is IGNORED — so `cd` to the dir holding the right `diskdefs` (e.g. `scratch/rc759-pce/images/`, format `drc-rc759`) BEFORE running them.**
- **[No stale dump files](feedback_no_stale_dump_files.md) — HARD: `rm -f` the artifact BEFORE the producer, every iteration**
- **[No DOTALL backtracking on source](feedback_no_dotall_backtracking.md) — HARD: no `re.DOTALL` + non-greedy over multi-line source; kill scans >10s**
- [z80 tree has no untrusted hooks](feedback_z80_tree_no_untrusted_hooks.md) — `cd … && git …` is safe; don't hedge
- [Docker Trace Disk](feedback_docker_trace.md) — z88dk-ticks -trace pipes through tail
- [CRLF on CP/M disk text](feedback_crlf_cpm_disk.md) — CR+LF when injecting text into disk images
- [Transcribe image PDFs](feedback_pdf_transcribe.md) — text version in repo when scanning image PDFs

## 8. Test / debug discipline

- **[Boundary codegen needs a runtime fixture](feedback_boundary_codegen_needs_runtime_fixture.md) — HARD: switch/range-check/compare-narrowing/off-by-one codegen ships a RUNTIME FIXTURE exercising min/max/just-past — the load-bearing safeguard, an oracle INDEPENDENT of your reasoning. A lit test alone is insufficient and "derive CHECK from first principles" does NOT save you (the blind spot is in the reasoning). Origin: #86 jump-table off-by-one, lit-only, cemented ~2.5mo.**
- **[Revalidate concern, not filename](feedback_revalidate_concern_not_filename.md) — HARD: file moved / workaround in place ≠ resolved; verify the symptom in CURRENT source**
- **[Outlier-first, not sweep](feedback_outlier_first_not_sweep.md) — HARD: dig ≥1.5×/≥50 B divergences; don't touch every difference**
- **[Verify matrix before theory](feedback_verify_matrix_before_theory.md) — HARD: contradictory cell pattern = stale state; clean + re-verify anchors first**
- **[Compilers agree means harness](feedback_compilers_agree_means_harness.md) — HARD: identical failure on both compilers → suspect harness/wiring first**
- **[Compare total section sizes](feedback_compare_total_section_sizes.md) — HARD: sum .text+.rodata+.data; per-function .text hides jumptables**
- **[No mental arithmetic in fixtures](feedback_no_mental_arithmetic_in_fixtures.md) — HARD: tool or trivial math only for expected values**
- **[Auto-kill stale daemons](feedback_kill_stale_servers_on_test_target.md) — HARD: test targets auto-cleanup leftover daemons (BYE, then SIGTERM)**
- **[Diff binaries before blaming codegen](feedback_diff_binaries_before_blaming_codegen.md) — HARD: `cmp -l` FIRST; byte-identical = environmental**
- **[Verify writes before chasing reads](feedback_verify_writes_before_chasing_reads.md) — HARD: instrument around the store first**
- **[Recognize ROM-shadow byte patterns](feedback_recognize_rom_shadow_patterns.md) — HARD: structured wrong bytes → `xxd` the PROMs first; match = memory-map bug**
- **[No taps inside polled-RX hot path](feedback_no_taps_in_polled_rx.md) — HARD: per-byte debug TX overruns SIO FIFO; per-frame markers or ring trace**
- **[A/B before blaming test-runner](feedback_ab_before_blaming_test_runner.md) — HARD: stash + rebuild + rerun baseline first; test_90/91 edge_*_O1 known noise (#136)**
- **[Baseline before implementing](feedback_baseline_before_implementing.md) — HARD: capture control measurement on UNMODIFIED system first**
- **[Value oracle covers all TRANSPORT cells](feedback_value_oracle_all_transport_cells.md) — HARD: SNIOS/xport/compat.h changes runtime-test every linking cell**
- **[Extract rules from time-sinks](feedback_extract_rules_from_time_sinks.md) — HARD (meta): after long debug sessions, propose new memory rules proactively**
- **[Multi-pass marker interactions](feedback_multi_pass_marker_interactions.md) — HARD: when an optimization "should fire" but doesn't, `-print-after-all` and check if the trigger marker existed earlier and was stripped mid-pipeline**
- **[Verify PASS condition before trusting green](feedback_verify_pass_condition.md) — HARD: cross-check elapsed time + artefact evidence; unexplained workaround = red flag**
- **[Search workspace before fetching](feedback_search_workspace_before_fetching.md) — HARD: before downloading any asset, search the WHOLE workspace by filename first (assets often in sibling submodules)**
- [User guesses are not constraints](feedback_user_guesses_not_constraints.md) — treat as starting suggestion; probe-first
- ["Intermittent" is a hypothesis](feedback_intermittent_is_hypothesis.md) — falsify via data-content checks before chasing timing
- [Integration Tests Expensive](feedback_integration_tests.md) — full suite only before merge/PR
- [Zoo Fast First](feedback_zoo_fast_first.md) — quick subset first
- [Verify DELAY_T](feedback_delay_tstates_test.md) — DELAY_T must match actual inner-loop T-states
- [Poll don't sleep](feedback_poll_dont_sleep.md) — poll completion markers, no long fixed sleeps
- [Canonical targets > enumeration](feedback_canonical_targets_over_enumeration.md) — `ninja check-<x>` over enumerating tools
- **[ticks canonical exit = ED FE trap](reference_ticks_canonical_exit_trap.md) — HARD: ED FE syscall (A=CMD_EXIT, L=exit code) not HALT; `-output` bypassed; grep existing project harnesses before deriving termination from scratch**

## 9. Code & source style

- **[Clarity in C code](feedback_clarity_in_c_code.md) — HARD: readable call shapes; compiler glue confined to hal.h/intrinsic.h, never #ifdef in business logic**
- [Size over speed for cold paths](feedback_size_over_speed_for_cold_paths.md) — cold code: bytes are permanent, T-states aren't
- [Volatile blocks loop idiom](feedback_volatile_blocks_loop_idiom.md) — volatile only for ISR-shared or hardware-changing memory
- [No self-correction in published docs](feedback_no_self_correction_in_published_docs.md) — narrate self-checks in chat; publish only the final version

## 10. Project facts — RC702 hardware

- **[RC700 family + PROM inventory](reference_rc700_family_proms.md) — RC701/702/703; RC701 has DIFFERENT ports + NO semigraphics (MAME emulation needs code changes); we target RC702/ROA375; RC701 PROM source likely lost**
- [No RC700 HW mods](user_no_hw_mods.md) — no PCB modifications; cables/external devices OK
- **[2 KB PROM hard limit](project_rc702_2kb_prom_hard_limit.md) — HARD: no A11 bridge; PROM0+PROM1 capped at 2048 B each; never propose "close A11"/"use 2732"**
- [SIO-A fast TX, no fast RX](project_sioa_tx_only_fast.md) — 614 kbaud TX verified; fast RX impossible (no DPLL)
- [Two Picos available](project_pico_count.md) — one on cbl923 keyboard rig, second for J3 CP/NET bridge

## 11. Project facts — cpnos / cpnet / DRI

- **[Long-term goal: finish rcbios + autoload-in-c + CP/NET + cpnos](project_finishing_firmware_components.md) — bias work toward measurably advancing one of the four**
- **[cpnos PARKED — awaiting physical parallel cable](project_cpnos_parked_awaiting_parallel_cable.md) — surface before acting on cpnos/PIO/polypascal tasks; unpark on user signal**
- **[MP/M disks: local-only, library frozen](project_mpm_disks_local_only.md) — `make mpm-disks` (cpnos-in-c) builds all tailored disks into disks/local/; NEVER write disks/library/; slave netboot image is RC700.NOS (was cpnos.img)**
- **[SDCC slave stack-room ceiling](project_sdcc_slave_stack_room.md) — cpnos SDCC PROM1 resident must end <= 0xF60E or SP=0xF680 stack overruns resident SNIOS data -> netboots but hangs at cpnos.sys handoff; check_sdcc_stack_room.py guards the build. sdcc = MAME-only secondary; clang = production**
- **[rcbios CP/NET PIO polypascal PASS 16s](project_rcbios_cpnet_pio_race_parked.md) — FIXED: z80pio 2eb88cea + snios RECVBY_PIO timeout; test: H:→PPAS→P PRIMES→PRIMES.COM→TESTDONE; ravn/mame#13 upstream candidate**
- **[SEM702 chip-photo request](project_sem702_request_chip_photo.md) — when the RC702 is open, ask for photos of the piggyback boards (ic82 + under ic68)**
- **[User's RC702 HAS SEM702, not ROA327](project_user_rc702_has_sem702.md) — define_sextants() is essential on the user's hardware (~79 ms, accepted); don't gate/remove it**
- [DRI NDOS — no upstream](project_dri_ndos_frozen.md) — we own cpnet-z80 DRI sources, edit freely
- [CP/NOS no local floppy](project_cpnos_no_local_floppy.md) — payload stays diskless
- [Fast link is CP/NET-only](project_fast_link_cpnet_only.md) — fast transport carries CP/NET + CP/NOS frames only
- [Z80 simple, host complex, hardware-compatible](project_z80_simple_host_complex.md) — push protocol work to host; must run on physical RC702
- [HiTech port parked](feedback_check_hitech_park_note.md) — read `tasks/hitech-port-parked.md` before proposing HiTech
- **[AES corpus = parity oracle](project_aes256_corpus_goal.md) — drives clang↔zsdcc parity + upstream bug queue; clang now dominates; gaps in `tasks/all-modes-competitive-plan.md`**
- **[AES K&R speed gap accepted](project_aes_kr_speed_gap_accepted.md) — clang +51% slower than SDCC on AES K&R `09_Oz_prod_like` post-sound-gate; structural CVP-strips-marker chain; off the critical path for the four finishing-firmware components**

## 12. External-bug refs

- [ravn/mame#6 — PIO-B slot regression](project_ravn_mame_6.md) — gates Option P bring-up; check before resuming cpnet-fast-link
- [ravn/mame#6 — workarounds failed](project_ravn_mame_6_workarounds_failed.md) — Paths 2+3 failed; fix needed at chip/slot layer

## 13. Reference / one-offs

- **[Canonical test aggregator](reference_run_all_tests.md) — `tasks/tools/run-all-tests.sh` runs all 4 test groups (lit / test-runner / z88dk run_all / softfloat) from one place; run at merge/checkpoint (canonical script, NOT a hook). `fast`=A+C+D, no-arg=full.**
- **[z88dk llvmz80 evaluation doc](reference_z88dk_evaluation_doc.md) — living document for z88dk project at `tasks/z88dk-llvmz80-evaluation-2026-07-21.md`; update after any bridge/benchmark/float change**
- **[copt is a 32-bit-int engine](reference_copt_32bit_eval.md) — z88dk copt HAS `%eval(<rpn>)` arithmetic (>>,<<,&,|,+-*/) but the whole evaluator is 32-bit `int` (stack/strtol/`%d`), so it CANNOT split a 64-bit `.quad` (verified 2026-08-05, ravn/z88dk#27). [Historical: the `splitquad.pl` external pre-pass was the first fix; superseded 2026-08-05 by the backend split — see next entry.]**
- **[64-bit `.quad` init split in the backend](reference_quad_init_backend_split.md) — ravn/z88dk#27 FIXED 2026-08-05 by `Z80MCAsmInfo` (ELF class) `Data64bitsDirective = nullptr`; `MCAsmStreamer` then emits two little-endian `.long` per 8-byte value, so no `.quad` reaches the bridge and `splitquad.pl` was deleted. Textual `-S` only (ELF object path unaffected). Lit `quad-init-split-27.ll`.**
- **[Standing goal: z88dk full llvmz80 CP/M support for rc700, latest clang C](project_z88dk_llvmz80_full_support_goal.md) — prioritize closing evaluation-doc gaps + modern-C support over tangential work**

- **[xcc issue-filing process](xcc-issue-filing-process.md) — retro-vault/xyz has issues DISABLED → file bugs as PR from ravn/xyz fork; repro goes in `x/tests/repro/` (their red-but-ignored convention, no xfail in cases/); root-cause via `xcc -S` (.ds vs .dw), GREEN oracle via docker gcc; libc/printf not auto-linked in the binary release. First bug: retro-vault/xyz#2**
- **[ez80clang comparison oracle](reference_ez80clang_oracle.md) — CEdev `ez80-clang` (SelectionDAG eZ80 fork) as corpus 6th lane, CODE-QUALITY only; `setup_ez80clang.sh` + `EZ80CLANG_ORACLE_SETUP.md`; needs z88dk clang_rules.1 fix (branch rc700-gensmedet-1); 3 cells skipped (sieve/fannkuch codegen cliff, pi 32-bit runtime gap) → rc700-gensmedet#122**
- **[RC703 TFj BIOS oracle](reference_rc703_tfj_bios_oracle.md) — datamuseum Bits:30003297 system tracks = a runnable assembled RC703 BIOS (`rel. TFj`); byte-level oracle for rcbios; system tracks preserved in rc703-div-bios-typer/**
- [User Profile](user_profile.md) — experienced dev, Z80/LLVM/SDCC, CLion, Docker, no brew
- **[TODO-later: cpmish distribution](project_cpmish_todolater.md) — undersøg davidgiven/cpmish som distributions-vehicle for rc702-8dd/5dd + rc703-qd diskbilleder**
- [TODO-later: z88dk RC700 wiki](reference_z88dk_rc700_wiki.md) — opdater github.com/z88dk/z88dk/wiki/Platform-Regnecentralen-RC700 med nye diskformater + llvmz80; trigger ved upstream PR
- [z88dk +cpm -subtype=rc700 build](reference_z88dk_rc700_subtype_build.md) — rc700.lib IKKE shippet (byg: `make -C libsrc TARGETS=rc700` + install); llvmz80-rute virker på niveau med SDCC; disk skal være eksplicit (-Cz+cpmdisk -f rc700-8dd); grafik = gencon 2x3-sekstant 160x75; eksempler i examples/rc700/
- [CANDIDATE BUG: llvmz80 miscompiles graphics.h calls](project_llvmz80_z88dk_callee_graphics_miscompile.md) — __smallc __z88dk_callee kald mishandles (sccz80 OK, llvmz80 dropper de fleste); blokerer <graphics.h> under llvmz80; ikke analyseret/filed endnu
- **[FILE* test suite parked — newlib migration](project_fileio_suite_parked_newlib.md) — 4 XFAIL + 9 SKIP genoptages når z88dk newlib CP/M file-driver lander upstream (ravn/z88dk#34)**
- [Host: sonnyboy](reference_host_sonnyboy.md) — Ubuntu 26.04 x86_64, `/home/ravn/z80`, headless, gh authed, upstream LLVM at `~/llvm-upstream/llvm-project`
- [Safari breaks claude login callback](reference_claude_login_safari_workaround.md) — use ANTHROPIC_API_KEY or non-Safari default browser
- [HiTech zc Docker image](reference_hitech_zc_docker.md) — `ghcr.io/ravn/hitech` provides `zc`
- [COMAL80 language manual](reference_comal80_manual.md) — RCSL 42-I-1758 @ Bits:30000018 (Dec 1981, OLDER than disk rev 1.07: no CHAIN/EXTERNAL); explains why .PRG apps won't load
- [DRI C Programmer's Guide location](reference_dri_cpm86_manuals_location.md) — DR C 1.11 ref PDF+txt already at cpm86-crossdev/docs/manuals/DRI_C_Programming_86.pdf (md5 41db5709…); do NOT re-download
- [DR C float/8087 ABI](reference_drc_float_8087_abi.md) — `-f` needs an 8087 (RC759 has none → default = software float, 0 ESC); double returns in DX:CX:BX:AX, float in BX:AX; all FP done in double
- [wlink vs DR C OMF/.L86](reference_wlink_drc_omf_l86.md) — wlink READS DR OMF modules fine (all 131 of CLEARL.L86 parse clean); REJECTS .L86 container (E2012, 0xA4 header not 0xF0/AR). Unpacker: unpack_l86.py. Native CMD → new loadcpm.c modeled on loaddos.c
- [CP/M-86 runtime RAM alloc + wlink FARHEAP knob](reference_cpm86_runtime_memory_and_farheap.md) — BDOS 53-58 (MC_ALLOC/MC_MAX/MC_FREE) are in BOTH plain CP/M-86 AND Concurrent (System Guide Jun83 §4.4; emu2 impls 55/53/57, NOT abs 54/56). `.CMD` M=G-Min floor / X=G-Max ceiling; X=0FFFF grabs ALL (bad for multitasking) → default X=M. wlink DATA group is fixed G-Min==G-Max (near arena = bloat #24); **`OPTION FARHEAP=<bytes>`** = the RAM knob: type-3 Extra, G-Length=0 (unstored), G-Min=1, G-Max=ceiling, loader grants actual free; farheap.c seam + fmalloc under `-mc`. Single user knob = FARHEAP via owcc (mirror `-mstack-size=`→OPTION STACK) or GENCMD X-edit fallback.
- [emu2 MCB pool must be TPA-capped at runtime, not just load-time](reference_emu2_tpa_pool_fidelity.md) — emu2's whole MCB free pool (sized once in `dos.c init_dos()`) is now capped to `CPM86_TPA_KB` for CP/M-86 programs (was previously only applied to the loader's load-time group grant, so runtime BDOS-128 `_fmalloc` calls could always find "extra" room MAME wouldn't have). New `-m <kb>` CLI flag (precedence over `CPM86_TPA_KB` env var, default 210 KB); `-m 190` reproduces MAME's exact `zip error: Out of memory (window allocation)` byte-for-byte.
- [CP/M-86 .CMD header + RC759 loader contract](reference_cpm86_cmd_header.md) — 128B header = up to 8×9B group descriptors (type/length/base/min/max paras), images padded to 128B records, 0x7F bit7=fixups. **MEASURED on real RC759:** small model → loader sets **DS=ES=data group** (DS=CS+code_paras), CS=code, IP=0; crt0 must NOT touch DS/ES. Data at DS:0100 (base page 0-0xFF; needs `op dosseg`+BEGDATA 0x100). **96-byte scratch stack is BY DESIGN** (spec §4.1.2) — crt0 switches to SS=DS/SP=top; a bigger loader stack needs a type-4 STACK group (wlink emits only 1/2 so far). 8080 model → IP=0x100, org 100h required. Native format cpm86 RUNS on real RC759 (fork branch wlink-cpm86-cmd-format, commit 7014acfb)
- [Watcom→DR C ABI bridge](reference_watcom_drc_abi_bridge.md) — **RETIRED 2026-08-19** (hybrid build/test pipeline in scratch/rc759-cmd-toolchain removed; owcc -bcpm86 is the sole Watcom path; genuine DR C oracle kept). Historical: `-ecc` (cdecl) + `#pragma aux default "*"` (no underscore) bridged Watcom→DR C. Return regs by type (bwdis-verified): int/near-ptr AX; **far-ptr BX:AX** (DRC_PTR); long/float BX:AX (DRC_LONG); double DX:CX:BX:AX (DRC_DBL). `_preincl.h` co-locates prototype+pragma (single source of truth). Library conformance suite `drc-libtest/` (7 tests PASS both models via differential DR C oracle; float BLOCKED by nofloat stubs) — coverage in drc-libtest/COVERAGE.md
- [DR C toolchain architecture](reference_drc_toolchain_architecture.md) — LINK86 = DR's shared system linker (bundled in drc86111, NOT C-specific); DR C = DRC compiler (→OMF .obj) + RASM86 + LINK86 (→.CMD) + LIB86 + CLEARS/CLEARL.L86 runtime. Pipeline: drc -b → rasm86 → link86 prog=srcfile
- [Official RC759 DR C v1.11 disk](reference_rc759_official_drc_disk.md) — pristine Regnecentralen oracle (datamuseum.dk IMD bits 30005869). Extract via imd2raw.py + cpmtools diskdef `rc759-drc` (./diskdefs, DISKDEFS env IGNORED). → rc759-drc-official/ (has real LIB86.CMD). drc-oracle.sh now defaults to it; codegen passes byte-identical to drc86111, hobby port only patches ~9 serial bytes. VERIFIED end-to-end
- [Watcom↔DR C / Aztec interop RETIRED — DR C is oracle only; owc-drc BANNED unless asked](reference_watcom_interop_retired_drc_oracle.md) — user 2026-08-17: ALL efforts to make Open Watcom work TOGETHER WITH DR C or Aztec are DROPPED now that standalone Watcom CP/M-86 works. DR C stays the ULTIMATE ORACLE for the .CMD file format + how memory models are implemented — to make standalone Watcom do the SAME, NOT to interop/link them. **HARD RULE: never use owc-drc in any build/benchmark/comparison unless the user explicitly asks — for "DR C" use the genuine DRC.CMD (drc86111) under emu2, not the owc-drc hybrid.** owc-drc/owc-drlink retired; bridge notes are historical.
- [RC759 i82730 CRT display (MAME) + DDHF image inventory](reference_rc759_i82730_display.md) — 2026-08-17, branch `rc759-i82730-cursor` (pushed ravn/mame, not merged). RC759 = STANDARD 62.5 Hz screen (82730 clock **916'500 Hz**), the 73 Hz screen was the RC750 Partner's; drives correct 16ms tick via tmrin0. load_row must consume EXACTLY MAX DMA COUNT words/row (auto_line_feed=0 menu vs =1 continuous boot console — over-read splits "READING…" over 2 lines). Amber text + dark-amber bg, i*7 pitch. Build: `make SUBTARGET=regnecentralen REGENIE=1 SOURCES=src/mame/regnecentralen/rc759.cpp OSD=sdl -j 10`. Open issues #22/#23/#24(blink 2× too fast, formula ignores DUTY CYC)/#25/#26. **DDHF originals in ddhf-cache/bits/: 30004107=SW1400_r3.1, 30004229=SW1400_r3.1a (4-disk CCP/M system sets, disk1.imd=boot).**
- [MAME crt-geom-flat bgfx chain](reference_mame_crt_geom_flat.md) — ravn/mame ships `bgfx/chains/crt-geom-flat.json` (crt-geom with curvature off = flat CRT look for RC702/RC759 amber); NOT active by default → `-video bgfx -bgfx_screen_chains crt-geom-flat`; no effect under `-video none`
- [simavr master required for .mmcu console](reference_simavr_master_required.md) — distro 1.6 is too old; build master in Docker
- [Memory in tasks/memory/, never ~/.claude/](feedback_no_claude_memory.md) — canonical here, read manually at session start
- **[Fingerprint build after 2 no-change edits](feedback_fingerprint_build_after_two_no_change_edits.md) — HARD: stop editing, add an undeniable marker, prove new bytes are running before edit #3**
- [MP/M II bakes RSPs into MPM.SYS at GENSYS time](reference_mpm_sys_baked_via_gensys.md) — `.RSP` edits inert until GENSYS regens `MPM.SYS` + re-installs on drive A:
- **[Never push/merge upstream remotes](feedback_never_push_or_merge_upstream_remotes.md) — HARD: cpnet-z80 origin is `durgadas311/*` (upstream); keep local commits FLAT, no `--no-ff` merges, no push. Only `ravn/*` repos get pushed/merged.**
- **[CP/NET 1.2 only](feedback_cpnet_12_only.md) — HARD: assume CP/NET 1.2 semantics; BDOS-105 is NOT forwardable under 1.2; ndos3.asm:504 (`db 0 ; 105 - can't support here, use SEND NW MESG`) is correct; time-from-master goes via BDOS-66/67 + FN-105 vendor extension (see `cpnet/todget/todget.c`).**
- **[rcbios jump table is ABI](feedback_rcbios_jump_table_is_abi.md) — HARD: BIOS jump table at 0xDA00 (incl. vendor extensions like 0xDA56 CLOCK) is frozen ABI for compiled CP/M programs on system disks. No delete, reposition, or stub of existing entries — even when a "better" wire path exists. New paths are ADDITIVE; legacy stays callable. New entries OK at end of table.**

- **[rcbios: never enable -flto (breaks boot-code placement)](feedback_rcbios_no_lto_boot_placement.md) — HARD: `-flto` merges C objects so `rc700_bios.ld` per-file matchers (`*boot_entry.o`, `*bios_hw_init.o`) fail; relocate_bios/verify/hw_init land high at 0xDA00 and `_coldboot` calls empty RAM → no `A>`. Fix = drop `-flto` (~15 B). Toggling a compile flag needs `rm *.o`, not just a relink.**

- **[Sieve-gap passes (sink + pin)](reference_sieve_gap_passes.md) — llvm-z80 has TWO opt-in default-OFF passes: `Z80SinkColdLoopIV` (`-z80-sink-cold-loop-iv`, M3, sieve −2.3% clean) + `Z80PinLoopPointer` (`-z80-pin-loop-pointer` + HLReg class, M5, net-regresses via scan-loop regalloc cascade). Don't re-implement. Trackers #256/#250/#251. Default-on RESERVED for user.**

- [llvmz80 classic-vs-newlib SPEED benchmark](reference_llvmz80_clib_speed_benchmark.md) — compiler fixed, lib varies: no single winner (classic qsort 1.5x faster, newlib sprintf 1.47x faster, string tie; newlib ~half size). qsort gap = CP/M newlib picks shellsort vs classic quicksort (__CLIB_OPT_SORT, tunable; +quicksort = -26% cyc/+194B, reverted). Cycle-accurate via ticks_cpm.py not ntvcm. Full: tasks/benchmarks/llvmz80-clib-speed-2026-07-26.md.
- **[z88dk native z80asm rebuild = 1-line Perl patch](reference_z88dk_native_z80asm_perl_patch.md)** — native macOS z80asm rebuild needs `make_lib_list.pl` `Modern::Perl`→`feature 'say'` (carried on master `64f43a9805`); rm stale empty `.lst` on refail; after upstream merges rebuild z80asm via `make -C src/z80asm ... install` THEN `./build.sh -b -p cpm`. The #3025 file driver only assembles under rebuilt z80asm.
- **[z88dk#3022 console-lost-after-fopen (CONFIRMED)](project_z88dk_3022_console_after_fopen_bug.md)** — newlib +cpm: fopen corrupts stdout, console output after fopen misrouted into the file (classic OK; sccz80+SDCC+clang all hit it → not a convention bug). Maintainer-confirmed "memory reuse, default subtype"; test PR #3031 open, feilipu fixes then merges. Flip fork XFAIL `test/clang/runtime_file_console.*` when merged.
- **[z88dk#3011 RC700 FP-under-interrupt = EXX collision](reference_z88dk_3011_fp_interrupt_exx.md)** — open Q from suborb: stock FP crashes under interrupts, 8080 mathlib works. Cause: math48 uses `EXX` 367×/genmath 21× as scratch → interrupted FP corrupted by any shadow-set-touching ISR (our `+shadow-regs` firmware qualifies). Confirm via DI/EI around the FP call. Draft reply parked, post only in user's voice.
- **[z88dk direction: classic forward, newlib deprecated](reference_z88dk_direction_classic_not_newlib.md) — MAINTAINER-STATED (z88dk/z88dk#3022): classic is the way forward, newlib is compat-only (being folded into classic); newlib file support NOT wanted (#34 stays unsupported). NEITHER lib is plain sdcccall(1) — public fns are __smallc/__z88dk_callee/__z88dk_fastcall; register-passing = __z88dk_fastcall; sdcccall(0) as the compiler convention beats per-call swapping. newlib's real edge for llvmz80 = it ships _callee/_fastcall variants clang calls directly (0 ex-de-hl adapters) vs classic's libsrc/l/llvmz80 adapter modules — but newlib is compat-only, so classic is the strategic target.**
- **[z88dk clang register ABI (ez80clang vs llvmz80)](reference_z88dk_clang_register_abi.md) — z88dk's `__CLANG` bridges were written for ez80-clang (eZ80, STACK args); ravn/llvm-z80 is z80 REGISTER ABI (HL/DE args, return in DE). The `defc ___X = X` string/mem bridges were wrong → hang; rewrote them in-place as `call asm_X; ex de,hl; ret` (z88dk commits bc1c0cd8, a452cd6c). strlen + single-arg fastcall class FIXED via header `__LLVMZ80` branch (12cfa587, 0292af1e). Don't split `__CLANG`. Bridges done for ~13 mem/str funcs; MANY str*/stdlib remain (see plan). Backend-HL-return would collapse all bridges to pure aliases.**
- **[clang double duty: ez80clang + llvmz80](reference_clang_double_duty_ez80_llvmz80.md) — BOTH z88dk clang backends define `__clang__`; any llvmz80 header/config branch MUST gate on `__LLVMZ80`, never bare `__clang__` (else it fires under the ez80clang oracle too). This is the newlib `_DEVELOPMENT/common/sys/compiler.h` trap (Phase C).**
- [newlib sdcc_iy links the sdcc_ix archive + IX audit](reference_newlib_sdcc_iy_uses_ix_archive.md) — `-clib=sdcc_iy` links sdcc_ix workers (only `--reserve-regs-iy` differs); clang's sole callee-saved GPR is IX, so the `__preserves_regs` risk = "does a public newlib entry clobber IX unrestored" → audited NO for stdio/malloc/str/atoi.
- [newlib clang integer helpers — CLOSED via llvmz80_imath.lib](reference_newlib_integer_helper_gap.md) — Phase C landed `-clib=newlib_iy/_ix` + compiler.h `__LLVMZ80` mapping. clang's gcc-style `__mulhi3`/`__divsi3`/`__divmodsi4`/`__mulsi3`/`__udivqi3` now provided by `z88dk/libsrc/l/llvmz80/newlib/llvmz80_imath.lib` (adapters over newlib-bundled `l_*` cores; wired into the CLIB lines). qsort/intdiv/long PASS.
- [z88dk newlib signed % drops sign — RESOLVED by lib rebuild](reference_newlib_signed_mod_z88dk_bug.md) — was a stale-prebuilt-lib bug (not clang); FIXED 2026-07-24 by merging upstream/master + rebuilding newlib libs (fix af5630797c). Merge left 2 classic clang regressions (#33 qsort, #32 strerror) — BOTH FIXED 2026-07-24 (see next). `xfail_signed_mod` now PASSES.
- [newlib IEEE-754 %f printf fix (#35) — split __mulsi3 + per-clib shim + __ZXNEXT trap](reference_llvmz80_newlib_ieee_printf_fix.md) — stock printf("%f") on newlib_iy + -D__LLVMZ80_IEEE_PRINTF now correct (z88dk cbbcc50031). 3 gotchas: split __mulsi3 (softfloat/imath dup), per-clib shim (classic shim bakes _sgoioblk), and the #ifdef __ZXNEXT placement trap (block silently skipped on CP/M).
- [newlib clang remaining gaps — #34 FILE* + #37 libm WONTFIX; #35 %f FIXED](reference_newlib_remaining_gaps_file_printf.md) — after Phase C the only remaining newlib_iy gaps are WONTFIX: #34 FILE* (CP/M newlib target has no file-open driver, maintainers want classic instead) + #37 libm. #35 variadic %f FIXED 2026-07-25 (`-D__LLVMZ80_IEEE_PRINTF`, z88dk cbbcc50031). classic printf_ieee skip + xfail_tmpfile are NOT product gaps.
- [llvmz80 classic qsort/strerror/bsearch fix — reversed-arg-alias-via-asm-label](reference_llvmz80_qsort_strerror_classic_fix.md) — #33+#32 fixed, commit 9e13c271c2 (classic clang 24 PASS/0 FAIL). qsort/bsearch: `stdlib.h __LLVMZ80` reversed-arg alias bound to `_qsort`/`_bsearch` via `__asm("name")` label (z80 re-prepends `_`) + swapping MACRO (inline would recurse) + `__smallc` comparator. strerror: add `__strerror_table.asm` to `llvmz80.lst` (was never in z80_crt0.lib; old "buildcrt glob" comment was false — z80nm proved it). bsearch xfail retired → runtime_bsearch.
- [z88dk lib rebuild is native (no Docker)](reference_z88dk_lib_toolchain_native.md) — CLAUDE.md "z88dk via Docker" note is STALE; bin/z88dk-{sccz80,zsdcc,z80asm} are native arm64. Recipe: `make -C libsrc TARGETS={cpm,z80}` + `install`; newlib `make -C libsrc/newlib cpm`; `*-clean` first (z80asm `-d` reuses stale .o). Libs gitignored (fix in source).

<!-- For project info that lives in the repo (not memory) — goal, TODOs, docs, MAME, PROM specs — see the "What's NOT in memory" map in README.md. -->

- **[M6: narrow i16 EQ/NE of byte sext (strrchr IY shuttle)](reference_m6_sext_icmp_narrowing.md)** — ravn/llvm-z80#259. `*s==(char)c` → un-narrowed i16 compare → IY shuttle. InstCombine misses the `ashr(shl x,8),8` sext-inreg idiom (narrows canonical `sext==sext` only). Z80-local GISel combine fixes -Oz/-Os only (LICM ordering); IR-level narrowing is the uniform fix (CHOSEN). No upstream report until verified end-to-end.

- **[z88dk runtime verify: ntvcm not ticks](reference_z88dk_runtime_verify_ntvcm.md)** — `+cpm` .COM under `ntvcm/ntvcm` for console output; `z88dk-ticks` does NOT emulate the `+test` `$ED$FE` console trap (no stdout). Idiomatic tests: `z88dk/test/clang/*.{c,sh}` via `NTVCM` env.

- **[Z80Pseudo undersize → far-`jr` under-relaxation class (#266/#267 + 14 latent)](issue267_pseudo_undersize_class.md)** — isPseudo pseudos that expand post-BranchRelaxation are sized 0 by getInstSizeInBytes → textual `.s` keeps out-of-range `jr` that z88dk z80asm rejects. #266+#267 fixed; guarded-LDIR/IDX8/MUL8/DIV8/SAT8 still latent.

- **[Pending canonical AGENTS.md edits](pending_agents_md_canonical_edits.md)** — cross-project rule changes discovered here but not yet applied to the canonical AGENTS.md repo. Currently queued: proactive oracle-coverage rule from #273 (every public entry point / sole helper user must run in the oracle).
- [Preserve reviewed commit](feedback_preserve_reviewed_commit.md) — on a PR under review, keep the reviewed commit as-is + add follow-ups; never squash it
- [RC702 MAME upstream PR #15805](project_rc702_mame_upstream_pr.md) — **MERGED** 2026-08-07; fork reconciled onto upstream
- [Session handoff 2026-08-07](project_session_handoff_2026-08-07.md) — UNPUSHED commits in 4 repos (mame/z88dk/rc700-gensmedet/workspace) + open ravn/z88dk#52 (fixed) #53 (rb+ open); push order + what was done
- [RC702 mame fork reconciled 2026-08-07](project_rc702_mame_fork_reconciled_2026-08-07.md) — master=upstream/master + 6 targeted commits; backup branch master-predev-2026-08-07; build needs OSD=sdl (SDL2) + SOURCES= must list pio_port files; cpnos-polypascal-test PASSES (needed RS232 38400 8N1 default restore + lua S03 DIP name fix)
- [llvmz80 double-reverse suspects (z80_outp, sem702_loadglyph)](reference_llvmz80_double_reverse_suspects.md) — same reversed-param __LLVMZ80 workaround as bdos #52; likely broken by #279; NOT fixed (silent-corruption class, need runtime verification via sem702-flip-test / port trace)
- [llvmz80 bdos() pointer-arg scramble — FIXED](reference_llvmz80_bdos_pointer_arg_scramble.md) — #279 (__smallc→z80_smallc) broke cpm.h's reversed-param bdos workaround; pointer arg became func number. FIXED (ravn/z88dk#52, cpm.h b9e7e72b98); guard issue52_bdos_ptr_abi. Both classic+clang qsort pass
- [z88dk __z88dk_callee/__smallc ABI mismatch class under -compiler=llvmz80](z88dk_z88dk_callee_llvmz80_abi_class.md) — bdos/bdosh + z80_outp + sem702_loadglyph fixed (commits 68a3462825, af7776f043): clang push-order is opposite of classic worker, and clang narrows uint8_t stack args to 1 byte while classic uses fixed 2-byte slots. Affects __smallc too. KEY: adding __smallc alone stops the hang but leaves args SCRAMBLED (silent wrong output, no crash) — always verify push order+width via -S or an end-to-end port/BDOS trace. ~1500 other decls still unaudited.
- [TODO: check zsdcc issues against upstream SDCC](todo_zsdcc_issues_check_upstream_sdcc.md) — the 8 open ravn/z88dk zsdcc bugs (#13/#12/#11/#10/#7/#6/#2/#1) are upstream-SDCC problems; find the SDCC project + verify each still reproduces on newest SDCC, then report upstream / close-if-fixed (per no-local-zsdcc-fixes). Batched separate pass, later.

- **[llvmz80 32-bit (long) return word-swapped vs classic clib](reference_llvmz80_32bit_return_swap.md)** — ravn/z88dk#51. 32-bit facet of the #50/#31 HL<->DE class: `clock()` (& all long-returning classic fns) delivered `ticks<<16`. clib returns HL=low/DE=high; clang default DE=low/HL=high. Fixed with existing `__z88dk_fastcall` on `time.h` clock() (commit aa9d9d8103, branch fix/llvmz80-graphics-hl-return, unpushed). clock() itself correctly reads BIOS-ISR 0xFFFC @50Hz. ~all long-returning classic decls still unaudited.
- [On crazy/garbage results, ALWAYS check the calling convention FIRST](feedback_check_call_convention_first.md) — user directive 2026-08-10. Insane values (garbage ints, word-swapped longs, scrambled pointers) => first hypothesis is an llvmz80<->classic-clib ABI mismatch; verify push order + arg slot + return reg (HL/DE) via `-S` and a sccz80 differential oracle BEFORE suspecting algorithm/emulator/race. Generalizes ferror/feof + bdos #52 + long-return #51 + __z88dk_callee class.
- [`__STDC_ABI_ONLY` dropped for llvmz80 (ravn/z88dk#55)](reference_stdc_abi_only_dropped_llvmz80.md) — branch fix/llvmz80-drop-stdc-abi-only (67bc26cee3), suite 61 PASS. Gate now `#if !defined(__LLVMZ80)` (ez80 __XCC keeps it). 3 blockers fixed: ctype __preserves_regs no-op, strerror int-errnum reorder, fputs/fputc excluded from _callee redirect. KEY: sccz80 `.c`-#asm callee workers (only fputs_callee.c/fputc_callee.c) are NOT genuinely callee-clean → SP drift/loop; the 146 pure-.asm callee workers are safe.
- [ravn/z88dk#54 seek+write NOT reproducible (CLOSED)](reference_issue54_seekwrite_not_reproducible.md) — random-access stdio write-back bug no longer reproduces on master (sccz80 + llvmz80). Regression fixture test/clang/runtime_fileio_seekwrite.{c,sh} landed (merge 3d4af968bd), suite 62 PASS/0 FAIL. Not root-caused why fixed.
- [ravn/z88dk#53 fopen "rb+" mode-parse FIXED](reference_issue53_rbplus_modeparse_fixed.md) — _freopen1.c parsed mode positionally so "rb+" opened read-only; fixed to scan +/b in any order (merge 68e18d08db). Absolute-assertion regression test runtime_fileio_rbplus.{c,sh} (fails on buggy, passes on fixed). Note: rebuild classic clib via `make -C libsrc cpm_clib.lib cpmixiy_clib.lib` + cp to lib/clibs/ (libs are gitignored).
- [ravn/z88dk#46 cross-TU rodata/string dedup gap (OPEN, missed-opt)](reference_issue46_rodata_cross_tu_dedup.md) — clang mergeable .rodata.cstN/.str1.1 not deduped across object files (copt bridge drops SHF_MERGE, z80asm only concatenates). Strictly cross-TU; size-only. Fix needs relocation-aware dedup in z80asm linker (substantial). Recommend measure production impact first.
- reference_issue57_freestanding_load_bearing.md — #57: -ffreestanding load-bearing on llvmz80 classic path (SimplifyLibCalls bypasses __smallc bridge -> garbage); keep the flag
- [z88dk clibs must be rebuilt manually after libsrc changes](feedback_rebuild_clib_manually.md) — lib/clibs/*.lib are gitignored artifacts; NOT auto-rebuilt. After editing libsrc: `make -C libsrc TARGETS=cpm && make -C libsrc TARGETS=cpm install`. zcc/test path never rebuilds libs; source-only commits ship stale libs. Check lib mtime > source before blaming a code regression.
- RC759 / DR C (CP/M-86 80186): DR C ABI + tooling summary at `cpm86-crossdev/docs/manuals/DRI_C_86_SUMMARY.md` (source PDF+txt alongside). Key ABI: args caller-pushed R->L on stack (multiword high-word-first), returns AX / BX:AX (long,far ptr) / DX:CX:BX:AX (double), preserve SI+DI, BP frame, NEAR(small)/FAR(big); externs have NO leading `_`, asm symbols UPPERCASE, 8-char significance; little-endian IEEE-754 f32/f64, char unsigned, no unsigned long; OS via `BDOS(func,arg)->char`. Small model = default (16-bit ptrs, code+data each <=64K).
- [RC759 MAME C-program verification harness](reference_rc759_mame_c_verification.md) — run mame-tests/run-mame.sh (build→cpmtools disk swap→boot→auto-stop on done-signal); regnecentralend MUST have FDC fix 59b21dc1312, rm nvram to autoboot, ~290s boot, install CMD as menu.cmd on mandel.img. **Done-signal:** guest calls mame_done(code) (mamedone.h) → OUT 0x2FE (undecoded); done_signal.lua io-write-tap snapshots + machine:exit() → stops in ~24s not 400s, host reads pass/fail from the word. Achieved 19/19 PASS int+file-I/O+loop-carried-long (not emu2). Oracle = snapshot PNG.
- [Watcom→DR C bridge: `long` mul/div in a loop — undefined __I4M, FIXED](reference_watcom_drc_long_loop_bug.md) — root cause was NOT writeback: Watcom's 32-bit helper __I4M/__I4D (cgsupp i4m.obj/i4d.obj) was UNDEFINED at link (DR C's CLEAR?.L86 lacks it), so `call far ptr __I4M`→~0 → hang(MAME)/wrong(emu2). Earlier single-long "passes" were const-folded. FIXED in cc-cpm86.sh (classicize+link i4m/i4d, broaden undefined-symbol guard). Verified: longloop r=10000, mtest 19/19 incl lfact/lpow10.
- [stdcbench 0.8 on CP/M-86 via Watcom→DR C bridge](reference_rc759_mame_c_verification.md) — `stdcbench-cpm86.sh -m s` builds all 16 modules clean (0 warnings) → **c90base=8 c90lib=5 final=13** on BOTH the Unicorn runner and real MAME rc759. Self-times via BDOS **T_SECONDS (fn 155)**, NOT the XIOS Int 28h fn 19 "16 ms counter" (that XIOS does not maintain it on the real disk — verified — so fn 19 was removed from both emu2 and the Unicorn runner). Blocker was port `inc/stdlib.h` declaring malloc/calloc/realloc `void*` vs target `_preincl.h` `char*` (E1062) — reconciled to char* (register-identical DR C ABI). Runs on real MAME via `mame-tests/scb-mame.sh` (done-signal, proof STDCBENCH_score13_mame.png). Large model unverified (ravn/rc7xx-work#4); math not covered (#3). Lives in open-watcom-v2 submodule.

- [Read MEMORY.md even on summary-resume; goal-change => re-survey toolbox+prior art](feedback_read_memory_on_summary_resume.md) — a compaction summary is NOT a session-start substitute; read the index before acting. Born 2026-08-14: replanned an Aztec CP/M-86 libc from scratch when MEMORY.md already recorded stdcbench final=13 via Watcom->DR C bridge (open: large-model rc7xx-work#4, math #3), and never asked what Watcom own clib provides (retargetable via thin low-level layer over BDOS). Same prior-art miss feedback_prior_art_before_own_fix was created to prevent.

- **[owcc CP/M-86 benchmarks use owcc STANDARD runtime only — NO custom seams](reference_owcc_cpm86_no_seams_softfloat_lib.md)** — 2026-08-19 standing decision by @ravn. All `-bcpm86` benchmarks build against owcc's own `cstartcpm.obj`+`clibs.lib` (which walks XI/YI init tables); NO crt0/cominit/stdioshim/lowlevel/stubs/errnoptr seams in any lib OR test source (tests stay plain C). Float support = ONE reusable **`cpm-soft-float.lib`** (genuine clib `setefg`/`seterrno`/`rtcntrl`/`iobaddr`/`istable` + `fpsoftstub` chip-markers + msdos.086 `fltused.obj` + the four msdos.086 clib component libs cgsupp/fpu/math/convert + merged math286.lib). Link: `owcc -bcpm86 -mcmodel=s -msoft-float prog.c cpm-soft-float.lib`. `-msoft-float`=wcc `-fpc` (never -fpi/-fpi87; RC759 has no 8087). ALSO: Unicorn `run()` must execute at FULL SPEED for plain output runs (no per-instruction/per-block Python hooks unless counting insns/cycles).
- [Watcom OWN clib printf retargets to CP/M-86 via thin BDOS seam — PROVEN](../../open-watcom-v2/contrib/ravn/watcom-cpm86-libc/README.md) — 2026-08-14: retarget route WON over both DR C bridge and Aztec recompile. Watcom's `__prtf` (bld/clib/streamio/c/prtf.c) is a pure formatter (0x INT21h) that emits via a callback; `cprintf.c` supplies a BDOS C_WRITE (INT E0h,CL=2) callback. Reuses Watcom __I4M/__U4M long-helpers + itoa/ltoa/lltoa/alphabet/str* UNCHANGED. Narrow ltoa/lltoa live in `ltoa.c`/`lltoa.c`; `ultoa.c` is the WIDE wrapper (#define __WIDECHAR__ + #include ltoa.c). Reproducible: `contrib/ravn/watcom-cpm86-libc/build.sh` (purity gate + emu2 oracle 123456*789=97406784). 16-bit gotcha: literal 40000 is a `long`, needs %lu not %u. Next: BDOS low-level shim (read/write/open/close/lseek/sbrk) → full stdio → relink stdcbench off DR C.

- [Watcom OWN near-heap retargets to CP/M-86 via a one-function arena __brk seam — PROVEN](reference_watcom_cpm86_heap_shim.md) — 2026-08-14, milestone 2 of rc7xx-work#6. Watcom small-model near-heap's ONLY OS trap is `sbrk.c` `__brk` -> INT 21h AH=4Ah (TinySetBlock). `port/lowlevel.c` swaps it for an arena bump over static _BSS + defines `_curbrk` (grownear.c reads it); crt0 seeds via wc_heap_init before main. All heap managers above (nmalloc/nfree/calloc/nrealloc/grownear/mem/_expand/nmemneed/nmsize/nexpand/nheapunl/bfree, +amblksiz/heapen/nheapmin) are UNCHANGED Watcom clib; heap sources need `-i=$B/clib/heap/h`. AVOID bmalloc/bexpand/growseg (based variant -> `__GrowSeg` -> DOS trap). Run-verified emu2 via build-heap.sh (malloc+qsort+calloc+realloc+free, 0x INT 21h). Fork bb1c7f68.
- Watcom full-optimization flag bundle is `/otexan` — use for the stdcbench score binary so the Watcom-vs-DR-C comparison is fair (user, 2026-08-14).
- **Watcom CP/M-86 IS a native, supported target — do NOT ever conclude otherwise.** wlink has native `format cpm86` (+ `small`/`8080` subformats, `LIBCPM86`): `bld/wl/h/_formats.h` MK_CPM86, `bld/wl/c/cmdcpm86.c`+`loadcpm86.c` (gated `_CPM86`), built objs `bld/wl/osxa64/{cmd,load}cpm86.obj`. The old `contrib/ravn/README-cpm86.md` "Short answer" WRONGLY said "cannot build CP/M-86 out of the box" (that misled a session 2026-08-17) — now FIXED to lead with native support; `build-cpm86.sh` is only the older freestanding raw+bin2cmd route. Runtime seam = `watcom-cpm86-libc/`. `build.sh` there was fixed 2026-08-17 (was missing `amblksiz.obj` → `__amblksiz` undefined; lowlevel.c's __brk seam refs it) — now links + PASSES emu2 oracle. (The old `build-cpm86.sh` freestanding raw+`bin2cmd.py` route was RETIRED 2026-08-19 — native `format cpm86` is the sole `.CMD` writer.)
- **Why `owcc -bcpm86` does NOT work fully out of the box (why contrib build.sh/build-lib.sh is needed) — investigated+verified 2026-08-17.** Two layers: (1) *tooling plumbing* is fully native+committed in git — `owcc -bcpm86` (`bld/wcl/owcc/osxa64/specs.owc` `system begin cpm86`) + wlink `system begin cpm86` (`bld/wl/lnk/.../wlink.lnk`) → `format cpm86`, `libpath %WATCOM%/lib286/cpm86`, `libfile cstartcpm.obj`. (2) but the *C runtime itself is NOT a target in Watcom's build system*: `find bld/clib -iname '*cpm86*'` = EMPTY (no cpm86.086 variant beside msdos.086/.286/.386), NO cstartcpm source under `bld/clib/startup/` (it lives only in `contrib/ravn/cpm86-clib/cstartcpm.asm`). The two artifacts `lib286/cpm86/{clibs.lib,cstartcpm.obj}` are build OUTPUT and **git-ignored** (`.gitignore:99 /*/`) → a `clean.sh` deletes them and nothing in the stock build regenerates them (that's how they "went missing"). So `contrib/ravn/cpm86-clib/build.sh` (seed) / `watcom-cpm86-libc/build-lib.sh` (full) are the stand-ins that hand-assemble the runtime from `port/*.c` + a few stock clib objs and install to `lib286/cpm86/`. Installed `clibs.lib` is only PARTIAL (132 syms: printf/fprintf/sprintf/strlen present; `putchar`/`getchar`/`gets` were MISSING until 2026-08-17 e58ee9b68c added them to build-lib.sh — bare `owcc -bcpm86` using putchar now links). Real "out of the box" fix = add a cpm86 clib+startup target under `bld/clib` so Watcom's own `builder` produces `lib286/cpm86/`.
- CP/M-86 has NO exact file byte length: only 128-byte sectors; text files use Ctrl-Z (0x1A) as EOF terminator, binary files have none (length known only to nearest sector). Any CP/M-86 write/read/lseek/FILE* shim must honour this (user, 2026-08-14).
- [Watcom CP/M-86 retarget: authoritative startup & init/fini mechanism (OW docs+src)](reference_watcom_cpm86_startup_initfini.md) — 2026-08-14. `docs/doc/cg/cromable.gml` ("Creating ROM-based Applications") OFFICIALLY blesses the retarget: use ROMable clib as-is (Layer 1), supply OS-dependent low-level fns write/read/open/close/lseek/sbrk (Layer 2), modify startup. Startup chain `CSTRT086.ASM`->`CMAIN086.C` (`_CMain`=`exit(main())`). Init/fini tables bracketed by `xiyi.asm` (`DGROUP group XIB,XI,XIE,YIB,YI,YIE`); `initrtns.c` `__InitRtns(255)` walks XI (runs `__InitFiles`), `exit()` `__FiniRtns` walks YI (runs `__full_io_exit` auto-flush). Resolves the stdio flush gotcha: proper crt0 = link xiyi.asm + `call __InitRtns` before main + terminate via Watcom `exit()` (not raw INT E0h); then NO per-caller fflush needed. Sole OS terminate seam = `__exit`->BDOS INT E0h CL=0.
- [Watcom OWN stdio FILE* write-path retargets to CP/M-86 via __qwrite+isatty seams — PROVEN](reference_watcom_cpm86_startup_initfini.md) — 2026-08-14, milestone 3 of rc7xx-work#6 (#7). build-stdio.sh runs Watcom's UNCHANGED buffered FILE* path (printf/fprintf/puts/fputs -> __fprtf/fputc -> __flush -> __qwrite) under emu2, oracle-matched, 0x INT21h. Seams in port/stdioshim.c: __qwrite (BDOS C_WRITE, bytes VERBATIM — fputc already did \n->\r\n) + isatty (replaces INT21h/44h IOCTL, line-buffers stdout). FILE has NO _base: _FP_BASE(fp)=fp->_link->_base; static __iob has _link=NULL so stdout is dead until Watcom's DOS-free __InitFiles (initfile.c, only lib_nmalloc) attaches a malloc'd __stream_link — link real initfile.obj + call once at startup. Fork dd3f048e.
- [Watcom OWN clib carries stdcbench 0.8 and BEATS DR C on CP/M-86 — PROVEN](reference_watcom_cpm86_startup_initfini.md) — 2026-08-14, milestone 4 of rc7xx-work#6. `build-stdcbench.sh` links the BYTE-IDENTICAL upstream stdcbench 0.8 integer suite (c90base+c90lib, same tree owc-drc built on DR C) against Watcom's OWN UNCHANGED clib + our seams — no DR C runtime. Only glue is `test/scbport.c`. Full surface: printf/sprintf, string+strtol, ctype (isdigit/isspace macros need istable.obj `_IsTable`; tolower is a fn), mem+memcmp/memchr, malloc/free/realloc/calloc, qsort, i4m/i4d. CLOCK: DR C used BDOS T_SECONDS(fn155) which emu2 lacks (spins); scbport reads T_GET(fn105) which BOTH emu2 (host clock) and RC759 XIOS maintain, so one binary times on both. emu2 run = FUNCTIONAL only (host-speed score); RC759-comparable score needs cycle-accurate MAME rc759 (`run-stdcbench-mame.sh`, -DMAME_DONE + OUT 0x2FE done-signal, reuses scb_mame.lua). RESULT on MAME (CCP/M 3.1, XIOS 2.3), Watcom full-opt `-otexan`: **c90base 12 · c90lib 8 · final 20** vs DR C **8·5·13** — Watcom ~1.5x faster. stdcbench exercises NO float → does NOT retire #3 double ABI seam.
- [Adding interrupt routines on CP/M-86 = direct segment-0 IVT poke (for the 8087-emulator float seam, rc7xx-work#8)](reference_cpm86_interrupt_vector_install.md) — 2026-08-15. AUTHORITATIVE from in-workspace DRI manuals: **CP/M-86 System Guide (Jun83)** (`open-watcom-v2/contrib/ravn/CPM-86_System_Guide_Jun83.txt`) BIOS "Setup all interrupt vectors in low memory" sample = there is NO BDOS set-vector call; write the far pointer straight into physical segment 0: INT N vector at `0000:N*4` (offset word) + `N*4+2` (segment word). Reserved IVT = 0–3FFH (256×4), always present/writable, excluded from the transient's MRT. BDOS entry = INT 224 (0E0H). **Concurrent CP/M-86 guide** §3.4: children inherit vectors 0,1,3,4,224,225 that the parent initialized → transients set their own vectors directly. 8087-emulator INT 0x34–0x3D are SYNCHRONOUS software traps (inline ESC/FWAIT), same process context → plain IVT poke suffices, DEV_SETFLAG/WAITFLAG (async HW) NOT needed. Watcom `initemu.asm` `__init_87_emulator` fills i34off/seg…i3doff/seg then `xchg_vects` (INT 21h fn35/25h — the ONLY DOS coupling, FATAL here); our port/ CP/M variant copies that 40-byte table into `0000:00D0..00F7` directly. Do NOT link the unmodified initemu.asm (static INT21h bytes trip the purity gate). MAME rc759 is the true oracle (emu2 may not model no-8087 faithfully). DR C installs its 8087 emulator on CP/M-86 the same way.
- [Watcom OWN double soft-float runs on CP/M-86 via `-fpc` — no 8087, no IVT (rc7xx-work#8, PROVEN)](reference_watcom_cpm86_softfloat_fpc.md) — 2026-08-15, milestone #8. **CORRECTION of the IVT note above:** `-fpc` emits plain `call __FDA/__FDS/__FDM/__FDD`+`__FDI4` with **0 ESC, 0 INT traps**; `fdmth086.asm` runs the pure-software `__FDxemu` branch because our `port/fpsoftstub.asm` sets `__real87=0`. So the shipped no-8087 build needs **NO emulator and NO IVT poke** — the INT 0x34–0x3D route (`emu87cpm.asm`, `FLOAT_8087_EMULATOR.md`) is now DEFERRED-hardware only. `build-float.sh`+`test/floattest.c` PASS under emu2: purity `INT21h=0·BDOS=2·INT34-3D=0`, oracle `pi6=3141592 mul=40115 add=468 sub=242` (355/113). Gotchas: test uses **volatile** operands (else constant-folded → 0 `__FDx` calls; tripwire guards this) and must call `__InitFiles()`+`fflush` (else blank). 8087 hardware = DEFERRED to someone with a chip (`docs/8087_HARDWARE_SUPPORT_DEFERRED.md`); `-fpc` still needs MAME rc759 cross-check. Fork 1a9d85a4a0.
- [Watcom OWN transcendental libm + real %e printf run on CP/M-86 (Whetstone) — PROVEN; + the 80186 CPU gate](reference_watcom_cpm86_whetstone_libm.md) — 2026-08-15, milestone #9. build-whetstone.sh+test/whetstone.c run Whetstone on emu2 no-8087; all 10 %12.4e check values match an independent host `cc -lm` oracle. libm from prebuilt msdos.286 mathlib (140 objs archived to math286.lib; no msdos.086 mathlib exists) + pure-8086 clib libs; real float printf via setefg.obj `__setEFGfmt()` called from main() (crt0 doesn't walk init table). Closure adds seterrno/rtcntrl/iobaddr/istable + port/errnoptr.c (`__get_errno_ptr_`=&errno, avoid dup errno) + `___FPE_handler dd 0` in fpsoftstub.asm. **STANDING build gate `assert_no_286`** (user's durable request): disassemble every non-8086 lib object, FAIL if any 286-only opcode (arpl/lar/lsl/lgdt/…/verw) — RC759 is 80186. TODO: INT34-3D byte-scan disabled (false-positives on libm double constants; needs disasm-based replacement). Fork d98f624934.
- [Watcom OWN disk FILE\* path on CP/M-86 via an FCB random-record seam — PROVEN](reference_watcom_cpm86_diskio.md) — 2026-08-15, milestone #10 / rc7xx-work#7 disk half. build-diskio.sh+test/disktest.c run Watcom's UNCHANGED FILE\* fopen/fclose/fwrite/fputs/fprintf/fread/fgets/fgetc/fseek/ftell/remove against REAL CP/M-86 disk files under emu2: 511 self-checking VERIFYs → `DISKIO: PASS`, `INT21h=0·BDOS=15`. **Byte-exact SEEK_END** via local `dfile_t.len` (fixed record-rounding: 200-byte binary→256); **runtime LRBC** via BDOS fn12 version probe (CP/M 3+/CCP/M-86 exact binary len, plain CP/M-86 rounds). HONEST GAPS in `KNOWN_ISSUES.md` (new): LRBC read path emu2-only (MAME authoritative, unverified); no write-side LRBC persist. ONE seam `port/diskio.c` supersedes stdioshim.c + adds `_sopen/__qread/__qwrite/__close/__lseek`+`lseek/_tell/remove/unlink` over FCB **random-record** BDOS (READ RANDOM fn33/WRITE RANDOM fn34: record=pos>>7, off=pos&127; unwritten record=EOF→Ctrl-Z tail=text-EOF for free; set BOTH SETDMASEG fn51+SETDMA fn26). KEY: fseek/ftell call PUBLIC `lseek`/`_tell` (not `__lseek`) which drag the iomode table → diskio.c provides own thin ones. Link adds fopen/fclose/allocfp/freefp/fgetc/fgets/fread/feof + comtflag(_commode)/textmode(_fmode); unreachable TTY-branch stubs tolower/__flushall/getche + stub-__lseek exclusion all guarded `-DDISKIO_LSEEK` so other builds' stubs.obj is byte-identical; errno owned by stubs.c, `__get_errno_ptr` by errnoptr.obj (never both). Gold-standard oracle is Watcom's own `bld/clibtest/streamio/c/iotest.c` (needs tmpfile/tmpnam/fscanf/fopen("CON") first) — documented next step. Fork 5e2e3509bd.
- [UnZip 6.0 on CP/M-86 (small model) — STORED-correct; deflate hits a 64K DGROUP ceiling](reference_watcom_cpm86_unzip_port.md) — 2026-08-16, rc7xx-work#9. Info-ZIP UnZip 6.0 links the CP/M-86 OW clib and extracts **STORED** entries byte-correct with **passing CRC** under emu2; zero generic-source edits (only OS layer `cpm86/cpm86.c` + `-D` flags in `build-cpm86.sh`). ROOT CAUSE of the `bad CRC 00000000` (correct bytes, zero CRC): Deflate64 `WSIZE=65536` makes `zcalloc(16384,4)` = `16384*4` **wrap to 0 in 16-bit** → NULL near window (offset 0, writable so output is fine); `crc32(buf=NULL)` hits its init sentinel and returns 0. Fix `-DNO_DEFLATE64` (WSIZE=0x8000). Companion: `_amblksiz=16` + `-DINBUFSIZ=512` to fit the 32 KB window in the ~35 KB arena. CEILING: DEFLATE won't fit — 32 KB window + huft + ~22 KB Far message strings overflow the single 64 KB DGROUP. Diagnose "returns 0 for all inputs" as a NULL/0-len CALL, print the pointer. Fork 5116747 / OW master e3e9c6b61f.
- [Native wlink FORMAT CPM86 — Watcom emits CP/M-86 .CMD directly (phase 1)](reference_watcom_wlink_cpm86_format.md) — user 2026-08-15 dropped DR C LINK-86 AND Aztec; canonical CP/M-86 toolchain is now single-pipeline `wcc → wlink FORMAT CPM86 → .CMD`, no foreign linker/OMF-classicize. Native writer (bld/wl/c/{cmdcpm86,loadcpm86}.c in scratch/open-watcom-v2, ⚠ two OW trees, uncommitted) MAME-verified on RC759: phase-1 SMALL model only (CODE type1 + DATA type2, base=0, no fixups); 8080 single-group REJECTED (Proc8080 fatals). .CMD groups pack at PARAGRAPH (16-byte) not 128-byte record. DR C = oracle/history only.
- [Long-running processes -> output to a file, report progress from it](feedback_long_process_output_to_file.md) — user 2026-08-15: for long/slow commands (builds, MAME, benchmarks) redirect stdout+stderr to a named log FILE and summarize progress by reading it; don't tail/stream. Prefer harness-tracked background (or nohup + a tracked PID-waiter) so completion is auto-notified.
- [Concurrent CP/M Programmer's Reference Guide (DRI, Jan 1984) — cached](reference_concurrent_cpm_prog_ref_guide.md) — DRI ORIGINAL (doc 1034-2023, 357p) cached at open-watcom-v2/contrib/ravn/Concurrent_CPM_Programmers_Reference_Guide_Jan84.pdf; authoritative BDOS/XIOS/.CMD reference for native Watcom cpm86 work. NOT the Siemens reprint. Do NOT re-download.
- [DR "Assembler Plus Tools" for CP/M-86 (Nov 1983) — versions & relevance](reference_dr_assembler_plus_tools_nov1983.md) — genuine DR CP/M-86 toolchain added to workspace at `/Users/ravn/z80/cpm86-assembler-plus-november-1983/` (RASM86.CMD **v1.2** 10/31/83, LINK86.CMD **v1.2**, LIB86.CMD **v1.1**, XREF86.CMD **v1.1**, SID86.CMD **v1.x** 1982 [oldest], 8087DEF.A86 include; all serial 4007-0000-001720). READ.ME documents LINK-86 8087REQUIRED/CONDITIONAL/$MY switches + CLASS/GROUP/SEGMENT NOT FOUND errors, RASM-86 $NC (no-uppercase, for C) + 8087 opcodes via 8087DEF.A86, SID-86 no symbol files for LARGE/COMPACT/MEDIUM (use .MAP). Ties to KNOWN_ISSUES §8 "DDT86/SID86 debug symbols in .CMD"; real .CMD transients usable as emu2/MAME test programs. Authoritative manual (Programmer's Utilities Guide) NOT yet cached in workspace.
- [Watcom C++ -> CP/M-86: freestanding subset works today](reference_watcom_cpp_cpm86.md) — emu2-verified 2026-08-15: wpp compiles C++ to cpm86 + wlink FORMAT CPM86 links it; drop prebuilt 8086 C++ runtime (bld/cpplib/library/generic.086/ms/plibs.lib) on the cpm86 libpath. WORKS: classes/methods/virtuals/templates/new-delete(over malloc)/local objs. NEAR (1 crt0 fix): global ctors don't run (crt0 doesn't walk __wcpp data-init). NEEDS PORT: iostreams (headers+streams), exceptions (plbxs.lib+setjmp). Small-model 64K limit (phase-1).
- [Watcom C99 for cpm86 user code = owcc -std=c99 -> wcc -za99](reference_watcom_c99_za99_cpm86.md) — verified 2026-08-18: default owcc -bcpm86 is C89 (a `for(int i=...)`/mid-block decl fails E1009); `owcc -std=c99 -bcpm86` emits `wcc -za99 -bt=cpm86` and builds+runs (SUM-OF-SQUARES 55). `-za99` is UNDOCUMENTED (`:internal.` in bld/cc/gml/options.gml:1178); documented equivalent `-zastd=c99`. clib itself built with `-zastd=c99` strict form.
- [Watcom 16-bit cg never folds (long)a*b>>k into one IMUL — structural, not flag-gated](reference_watcom_i4m_imul_cg_gap.md) — 2026-08-16, source-verified in bld/cg. FP_MUL idiom → `call __I4M`+shift loop, no flag changes it (enh #9; 80186 angle #18). 3 layers: (1) i86optab OP_MUL is result-type dispatch, I4→RTN4C=__I4M vs I2→MUL2 hardware (386 I4→MUL4, so 16-bit-only gap); (2) front end never emits OP_EXT_MUL/emul widening multiply; (3) multiply.c strength-reduces by-constant only, native-word (WD/SW) only. Never fixed because it's structurally 16-bit-only + OW dev targets 32/64-bit. Flag ref: contrib/ravn/watcom-cpm86-libc/docs/WATCOM_FLAGS.md.
- [Reproducible full build of the OW submodule on Apple Silicon (CLANG)](reference_watcom_submodule_build_apple_silicon.md) — verified 2026-08-16 (M4): the whole toolchain builds INSIDE the submodule open-watcom-v2 with `OWTOOLS=CLANG OWOBJDIR=binbuild OWNOWGML=1 OWDOCBUILD=0`, then `sh clean.sh && sh build.sh`. Retires the old two-tree (+scratch) setup. `OWNOWGML=1` is MANDATORY (else FATAL at bld/browser building wbrw.gh via wgml→DOSBOX; OWDOCBUILD=0 alone doesn't skip it; build.sh is incremental so re-run resumes past it). Output layout: only wcc/wcc386/wdis under osxa64/binbuild/, rest directly in osxa64/ (wpp, wasm, wl/wlink[CPM86], wcl, nwlib/wlib). clibext ordering did NOT recur under clean CLANG build. ~40 min cold. Issue #11 (resolved).
- [cpm86-crossdev = fork of tsupplis/cpm86-crossdev (NOT ravn's own)](reference_cpm86_crossdev_fork.md) — the cpm86-crossdev submodule is ravn's fork of upstream tsupplis/cpm86-crossdev, a CP/M-86 cross-dev env (bin/ cpm86+emu2+Aztec/DRI wrappers, docs/manuals incl. DR C guide, diskdefs). NOT ravn-authored; ravn's own cpm86 material is under open-watcom-v2/contrib/ravn. Also copied at scratch/cpm86-tools/cpm86-crossdev.
- [Aztec C86 DOS toolchain — location, working one-liner, docs](reference_aztec_c_dos_toolchain.md) — 2026-08-17 VERIFIED: DOS Aztec C works today. `open-watcom-v2/contrib/ravn/cpm86-crossdev/` submodule, PATH=`bin/`, run `.exe` tools via emu2. aztec34 (C86 3.40a, **K&R** — `int main(void)` rejected) and aztec42 (**C86 4.10d / v4.2, ANSI, NEWEST** — accepts `int main(void)`, Pass1→cgen→as) BOTH FULLY installed & DOS-verified. 4.2 fetched from AztecC86.zip (cached in `archive/`). One-liner: `aztec4{2,4}_cc -I. t.c && aztec4{2,4}_link -o t.exe t.o -lc && emu2 t.exe` → real MS-DOS .exe. Model via lib: `-lc` DOS small / `-lcl` DOS large / `-lc86` CP/M-86 .cmd. GOTCHA: `-D__CPM86__`+`-lc` → "Code and data regions overlap" (don't mix). Codegen via `aztec3{4,2}_obd file.o` (4.2 uses hardware IMUL). Docs: in-zip `AZ8634B/DOC/README`, cpm86-crossdev/README.md + examples/Makefile; full manual online only (MS-DOS 4.10C PDF). STANDALONE codegen look, NOT retired interop.
- [CP/M-86 "big" memory model (DR C §2.4.2) fully documented — mechanics resolved 2026-08-18](reference_cpm86_big_model.md) — only 2 DR C models exist (small/big, `-b`, CLEARL.L86): FAR code (multiple separate ≤64K segments, NOT CGROUP, total=avail mem), NEAR DGROUP ≤64K, stack ≤64K (SS), heap in ES (grows up, ~1MB max, G-Type=3 Extra descriptor). Mechanism: `.CMD`'s "8 group descriptors" caps *sections* not segment count — LINK-86's CODE[SEGMENT[...]] concatenates many ≤64K segments into ONE Code section (up to ~1MB, G-Length is a 16-bit *paragraph* count). Section-number mismatch resolved: DR C manual's cited "Utilities Guide §7.5" = FlexOS 286 Utilities Guide's §7.7 — FlexOS 286 is literally the later renamed CP/M-86 (user), not just a linker-sharing sibling. Also covers far-vs-huge pointer semantics (Watcom `wmodels.gml`).
- [Plan: Stage A = port Watcom's existing COMPACT model (-mc) to CP/M-86; Stage B = code>64K deferred](../plan-cpm86-big-model-2026-08-18.md) — decided 2026-08-18: not a new mechanism, reuse Watcom's own `-mc` (small code + big/far data). Watcom's clib ALREADY redirects plain `malloc()`→`_fmalloc()` inside the compact-model library variant (`bld/clib/heap/c/fmalloc.c`, gated on `__BIG_DATA__`/`CGSW_X86_BIG_DATA`) — mechanism lives entirely in Watcom, zero user-code changes (explicit requirement). Tradeoff accepted: ALL pointers become 4-byte far under `-mc`, not just heap ones. Documented split: small=64K code+64K data(+heap+stack in DGROUP, unchanged); compact=64K code+64K data+separate far heap (Extra/ES segment). Heap size = link-time-only `LINK86 prog=srcfile [EXTRA[MAXIMUM[hex-paragraphs]]]` (real syntax found in DR C's own `drc86111/*.BAT`) — no runtime "ask for more heap" call exists. "Big model"(Stage B)↔Watcom-model-name mapping explicitly PARKED until linker behavior is understood.
- [Repo preToolUse hook fails-closed on one keyword in tool commands](reference_repo_pretooluse_hook_keyword.md) — 2026-08-16, bisected: a server-side "repo settings" preToolUse hook ERRORS (denies, fail-closed: "Denied by preToolUse hook ... (hook errored)") whenever a tool command OR a git commit message contains the substring l-o-c-a-t-e. Reword (derive/find/self-place) to proceed; don't name files/paths with it either (breaks later git add). Hook bug, not a policy — rewording is not circumvention.
- [CP/M-86 toolchain: how to build it all from scratch on Linux](reference_cpm86_toolchain_linux_build.md) — 2026-08-16 sonnyboy: OW bootstrap (`OWTOOLS=GCC`, boot stage only), build-cpm86.sh smoke tests, emu2-cpm86 (NOT dmsc/emu2 — no CP/M-86 support there), cpmtools (bare `./configure`, any `--with(out)-libdsk` flag forces a libdsk.h requirement), MAME `SOURCES=`-filtered rc759 build. cpm86run.py deleted (user directive) — use emu2-cpm86/Unicorn as the independent oracle instead.
- [Open Watcom FULL build (non-bootstrap) on Linux — result + log-size baseline + rel/install stage](reference_openwatcom_full_build_linux.md) — 2026-08-18: `./build.sh` no-arg SUCCEEDS completely once real DOSBox is installed (`apt install dosbox` + `OWDOSBOX=dosbox`). `builder build` only compiles (scattered under `bld/<tool>/<host-target>/`) — the actual install/consolidation is a SEPARATE stage, `./build.sh rel`, which copies the flat extension-stripped tool set into `rel/binl/` (`wcc386`/`wasm`/`wlink`/`wpp`/`wlib`...) and CP/M-86 clib into `rel/lib286/cpm86/`. PATH `rel/binl/` for real (non-bootstrap) tools.
- [CP/M-86 crt0 must switch SS=DS itself — real bug found+fixed 2026-08-16](reference_cpm86_ss_ds_entry_bug.md) — loader hands a small-model program a throwaway scratch stack with SS≠DS, NOT SS=DS; unmodified Dhrystone's `Proc_2(&Int_1_Loc)` silently missed the real stack slot on real MAME rc759 while passing under a too-lenient emu2. Fixed both `open-watcom-v2` `contrib/ravn/cpmstart.asm` (commit `dc3c9f9fd7`) AND `emu2-cpm86`'s loader (so emu2 stops masking this bug class) — PASS on both now. emu2-cpm86 fix is NOT durable (plain clone, not a submodule).
- [MAME regnecentralen: rc75x shared base, IMD is writable, blank disk = MFI](reference_mame_regnecentralen_rc75x_imd.md) — 2026-08-16. RC759 Piccoline + RC750 Partner both derive from a new shared `rc75x_state` base (rc75x.h/.cpp: 80186+8259A+8255+82730+MM58167+NVM+SN76489A+kbd); rc750 is its own subclass (NOT of rc759), MACHINE_NOT_WORKING (no Partner ROM dump; guide Appendix B is OCR-blank so ports provisional). Build: SUBTARGET=regnecentralen needs SOURCES= + `REGENIE=1` when adding files; stale-archive → `touch` the .cpp. **IMD IS read/write in this MAME** (imd_format::supports_save()==true, save() implemented at src/lib/formats/imd_dsk.cpp:709,981) so A: .imd edits persist — old "IMD read-only" lore is WRONG here. A *blank* disk must be MFI not IMD (IMD stores only formatted tracks; `floptool flopcreate imd u8dsdd` → unloadable "Unknown format"; raw .img → "Unable to identify"): use `floptool flopcreate mfi u8dsdd out.mfi`, format once under CP/M. Launcher scripts/rc702_boot_cpm.sh (top-level) mounts A: IMD + empty MFI B:.
- `reference_cpm86_vi_editor_feasibility.md` — lille vi/vi-klon (levee/STEVIE) til CP/M-86: målt feasibility, levee anbefalet (kode passer, DGROUP-trim nødvendig).
- [MAME rc759 headless on sonnyboy — from-scratch harness + diskdef corruption bug fixed](reference_rc759_mame_sonnyboy_headless.md) — 2026-08-18: rc759 driver+ROMs already present (no rebuild); no turnkey mandel.img here, so boot a plain DDHF disk (Bits:30002654 CDOS systemdisk) + natkeyboard-inject `A`/progname timed off `m.time.seconds` (~115s in); `-video soft` NOT `-video bgfx` under `SDL_VIDEODRIVER=dummy`. FIXED a real diskdef corruption bug in-flight: `scratch/rc759-cmd-toolchain/diskdefs`'s `rc759-drc` still had the WRONG maxdir 96/os 2.2 (2026-08-17 bug, see `reference_rc759_official_drc_disk`) — corrected in place to maxdir 512/os 3 before any cpmcp write.
- [watcom-cpm86-libc diskio.c: pure-reader-vs-open-writer stale-read bug FIXED](reference_watcom_cpm86_diskio.md) — 2026-08-18, commit `3f815e6c53`. Root cause of the streamio `iotest.c` "flushes" failure (line 577, confirmed identical on emu2 AND real MAME rc759 — genuine bug, not emu2 fidelity gap): a pure-reader handle's own `BD_READRAND` is unreliable once a DIFFERENT still-open writer handle on the SAME file has written past what CP/M's directory (synced only at `F_CLOSE`) reflects — it can report "unwritten" OR, worse, SUCCEED but return STALE data, clobbering the correct copy already cached in the shared `dma[]` buffer. `load_record()` now routes a pure reader entirely through the writer's FCB/cache, never falling back to its own FCB while an open writer exists on the same file. `build-owtests.sh` (float01-04) re-verified still green. Debugging note: `printf`-based tracing INSIDE `diskio.c` self-contaminates (iotest.c tests stdout redirection) — use raw `_bdos_conout` byte-writes for in-seam debug tracing instead.
- [emu2 variants — stock dmsc/emu2 (DOS tools) vs forked emu2-cpm86 (DR C headless)](reference_emu2_variants.md) — TWO distinct emu2 builds: crossdev `emu2/`+`bin/emu2` = stock dmsc/emu2 for DOS tools (Aztec, nested cpm86.exe); `scratch/cpm86-tools/emu2-cpm86` = fork ravn/emu2-cpm86 branch cpm86-drc-headless with 3 CP/M-86 patches, drives DR C 1.11 headless (used by drc-oracle.sh). For DR C ALWAYS use the fork, never crossdev stock (double emulation hangs on "Stop DRC (Y/N)?"). DR C code-gen prints `code: N` = function code bytes directly.
- [BDOS coverage gaps — emu2-cpm86 vs the Unicorn runner (COMPLEMENTARY, neither is a superset)](reference_cpm86_emu2_unicorn_bdos_gaps.md) — measured top-level BDOS sets (2026-08-21). emu2 = full-ish (file sys 15-36, MC 53-58, DMA 26/51, S_BIOS 50, P_CHAIN 47, F_PARSE 152) but MISSING the whole character-device family **3 AUXIN / 4 AUXOUT / 5 L_WRITE / 7-8 IOBYTE** (the redirection primitives!) plus file niceties 28/37/40/52. unicorn = console+compute+time only (0 1 2 5 6 9-14 25 104 105 155), by-design no file sys; it's the only one with L_WRITE(5). **UPDATE 2026-08-21: emu2 now has 104 T_SET + 155 T_SECONDS** (issue #14 DONE, ravn/emu2-cpm86 44270a1) — its BDOS clock was switched from host wall-clock to a deterministic instruction-count virtual clock (seconds = cpuGetInstructionCount()/CLOCK_HZ, env EMU2_CPM86_CLOCK_HZ default 300000), mirroring unicorn's code-byte clock (CPM86_CLOCK_HZ), so both give a reproducible work-proportional timer for stdcbench. T_SECONDS resolution is whole seconds (no sub-second TOD field, matches HW); precision = determinism + CLOCK_HZ tuning, NOT sub-second. unicorn ticks only advance in --count mode (FULL-SPEED RULE). Oracle: watcom-cpm86-libc/build-tsecs.sh (emu2 66->133 s, unicorn --count 186->373 s on HZ halve). Remaining emu2 gaps filed #13 (char dev 3/4/5/7/8), #15 (28/37/40/52); already-tracked #10/#11/#12. `case 3/4` in cpm86.c are S_BIOS SUBfns, not top-level BDOS 3/4.
- [Win16/Win386 host route — dead end for CP/M-86 work, verified 2026-08-18](reference_watcom_win16_host_no_new_leverage.md) — `rel/binw/wcc.exe` is LE-format 32-bit protected-mode (WIN386 extender), same class as `binp/` (DOS4GW), NOT a real 16-bit host. Official GitHub release `open-watcom-2_0-c-win16.exe` (repo open-watcom/open-watcom-v2, tag Current-build) is only a Win16-native NE *installer wrapper* — its payload bundles the identical `binl/binnt/binp/binw/` tree, no exclusive 16-bit-hosted compiler inside. Win16 clib's low-level I/O (`io086.asm`) is byte-identical to DOS clib's, no new seam to harvest. No leverage for the CP/M-86 retarget in any of the three angles checked.
- [Two-oracle split: MAME=CCP/M-86 software oracle (vs emu2/Unicorn); PCE=RC759 hardware oracle (vs MAME's own rc759 driver)](project_pce_rc759_vs_mame_accuracy.md) — user 2026-08-18. NOT a blanket "PCE beats MAME" — MAME still wins on CP/M-86-level correctness vs lighter emulators; it's specifically MAME's rc759 hardware driver that PCE (Hampa Hug's independent `src/arch/rc759/`) currently outranks. Submodule added at `pce/` (fork `ravn/pce` of `retrohun/pce`). Cross-check against PCE before concluding firmware/compiler output is wrong on a surprising MAME-rc759-hardware result.
- [PCE/rc759 headless automation — X11 key injection, monitor SIGINT break-in, proper disk-swap API](reference_pce_rc759_headless_automation.md) — 2026-08-18, full pipeline proven (boot→type command→swap floppy via `m disk.eject`/`m disk.insert`→screenshot), no file overwriting. Key facts: rc759 has NO ESC-hotkey monitor-break (ibmpc/macplus/atarist only); `kill -INT` breaks a live `g` back to monitor but has a real race (`pce_start()` resets `brk=0` when `g` starts — retry-send until a new log line confirms it landed); `c <N>` cycle-stepping is NOT faster than realtime (~6.25M cycles/sec ceiling); XWD screenshots parse fine with a 20-line Python struct/PIL script, no ImageMagick; X11 keys sent via raw `ctypes`+`libX11.so.6` `XSendEvent`, no xdotool/sudo needed.
- [Uniform MAME/PCE RC759 boot wrappers + genuine CCP/M-86 disk (not CDOS) + 4-console TODO](reference_ccpm86_boot_disk_and_4console_todo.md) — 2026-08-18. `scripts/rc759_boot.sh {mame|pce}` dispatches to `rc759_boot_cpm.sh`/`rc759_boot_pce.sh`, same `A_DISK`/`B_DISK` env-var interface, both now default to genuine CCP/M-86 (Bits:30004229 disk1, converted IMD→raw, cached at `scratch/rc759-cmd-toolchain/ddhf-cache/derived/sw1400-r3.1a-disk1.img`) — NOT CDOS. PCE builds B: as a plain raw `.img` (no floptool needed, unlike MAME's `.mfi`). Deferred TODO: this disk is an *installer* offering 1- or 4-console CCP/M-86 builds; no pre-built 4-console disk exists yet in the cached archives.
- [Never export a var named WCC/WASM/WLIB/WLINK — the Watcom tools read that exact name for default switches](feedback_watcom_tool_env_var_collision.md) — 2026-08-18: `export WCC=/path/to/wcc.exe` makes `wcc.exe` itself misparse its own path as a bogus 2nd source file (`E1139`), looking like nondeterminism until you check exported-vs-local. Wrapper scripts must use differently-named overrides (`OWCC_BIN` etc.) and defensively `unset` the collision names.
- [Concurrent CP/M-86 Extra-group grant is capped by fragmentation, not by the requesting program's own CODE+DATA size](reference_cpm86_big_model.md) — 2026-08-18: shrank farheaptest's own DGROUP footprint from ~48K to ~12K (see next line's arena finding) expecting the RC759 far-heap grant to grow correspondingly — it was BYTE-IDENTICAL instead (181,647 B/79 blocks, both "real out-of-memory"). So the ~33K gap between 261K brugerlager and what got granted is NOT explained by our own program's size; likely Concurrent CP/M-86's allocator hands Extra the largest free CONTIGUOUS block, capped by the 4-console system's other resident-state fragmentation. Not pursued further (out of Stage A's scope).
- [Watcom's OWN `_fmalloc()` has a near-heap fallback — falls back to `_nmalloc()`/DGROUP once the far heap is exhausted, disguised as a far pointer](reference_cpm86_big_model.md) — `bld/clib/heap/c/fmalloc.c` (`if (seg == _NULLSEG) { cstg = _nmalloc(amt); ... seg = _DGroup(); }`), a real Watcom feature, not a bug. Any test that loops `_fmalloc` until failure to measure far-heap CAPACITY must detect and exclude this (check `FP_SEG(ptr) == _getds()`) or it silently overcounts. `farheaptest.c` now does; also made `port/lowlevel.c`'s `WC_ARENA_BYTES` overridable via `-D` so far-heap-only tests don't waste ~36K of DGROUP on an unused near-heap arena.
- [Digital Research OS source archive (authoritative independent oracle) at https://www.cpm.z80.de/source.html](reference_cpm_dri_source_archive.md) — user-provided fact 2026-08-19: original DRI source for the whole CP/M family (self-signed TLS → `curl -k`). Downloaded `ccpm8620.zip` → `scratch/ccpm86-src/`; its transient **Program Load** routine `kern/load.sup` (+ `kern/cmdh.def`, `kern/basep.fmt`) is the authoritative oracle. **RESOLVED: the genuine CCP/M loader relocates `.CMD` fixups itself** (`load.sup:405-449` tests byte-127 bit 7, reads `ch_fixrec`, does `add es:[di],dx`) — reversing the interim emu2-based self-relocation guess (emu2 is only a reimplementation). Also has CP/M-86 1.1 BDOS, MP/M-86 2.0, CP/NET-80/86 sources.
- [wlink `format cpm86` — program far data goes in ONE type-3 EXTRA group (FIXED 2026-08-19)](reference_wlink_cpm86_far_data_type3.md) — `bld/wl/c/loadcpm86.c` (local commit `09c2eb3099`). Old writer marked EVERY non-code group type-2 DATA + fixup nibbles code/data-binary, so a program's initialised **far data** became a 2nd type-2 (unplaceable, fixups collide with DGROUP → 64K DGROUP ceiling, blocks compact model + UnZip DEFLATE). Fix: 3 buckets = all CODE→type-1, **DGROUP (identified via linker `DataGroup` ptr, NOT Groups-list position — OrderGroups can put FAR_DATA ahead of DGROUP, inverting them)→type-2 DATA**, every other non-code→ONE coalesced type-3 EXTRA. On-disk image order must match descriptor order → non-code write is 2 passes (DATA image then EXTRA). Fixup nibbles carry actual CMD type (far refs name type-3). Each far object's seg word relocated individually → EXTRA image may span >64K total (no single obj >64K). OPTION FARHEAP now raises the existing far-data EXTRA's G_Max instead of emitting a 2nd type-3 (collision). Verified: `__far` repro prints `FARDATA-OK` (was blank) on Unicorn; Stage B far-call/far-ptr + small-model + reloc-consumer + farheap-small oracles all pass; UnZip links CODE + type-2 DGROUP 29K + type-3 far 36K. Full UnZip runtime CRC still needs emu2 P_LOAD fixups (ravn/emu2-cpm86#1) or MAME.
- [CP/M-86 .CMD static checker + far-heap layout constraint](reference_cpm86_cmd_checker_farheap_layout.md) — 2026-08-19: `contrib/ravn/cmd_check.py` statically lints a `.CMD`'s group descriptors for the far-heap defects (exit 0/1/2; wired into build-compact-farheap.sh). **F1 far-heap/far-data OVERLAP**: our compact design puts program far data/BSS `[0,G_MIN)` AND `OPTION FARHEAP` `[G_MIN,G_MAX)` in one type-3 EXTRA group, but `farheap.c` carves slabs from paragraph 0 → first far malloc() overwrites the far data. SAFE (current carve-from-0 farheap.c) iff Extra is heap-only (`G_LENGTH==0 && G_MIN<=1`) — which is why SMALL-model far heap works and COMPACT doesn't. Fix direction (checker `--heap-starts-at-min` models it): carve from G_MIN, but base page lacks G_MIN → needs a separate heap group or a linker end-of-far-data symbol (OPEN). **DR C (definitive oracle) does NOT cover this**: genuine DRC.CMD/LINK86.CMD keep all data in DGROUP (≤64 KB) with NO Extra group, so far-data+far-heap coexistence is new UnZip-only territory. Also F2 (DGROUP>64 KB + `--map` near-arena-in-FAR_DATA), F3 (malformed descr), F4 (fixup sanity). Companion runtime oracle: `watcom-cpm86-libc/test/compact_farheap_test.c` (7 checks c1..c7).
- [CP/M-86 P_LOAD fixups — EXACT format, all 4 sources cross-checked](reference_cpm86_p_load_fixups.md) — 2026-08-19: the single source of truth for `.CMD` load-time relocation, grounded in genuine CCP/M-86 2.0 `kern/load.sup` (fx_chk loop) + `kern/cmdh.def:18-26` AND both reimplementations AND the wlink producer. Signal: header 0x7F bit7=fixups present, word 0x7D=`ch_fixrec` (file RECORD#, ×128=byte offset). Record = 4 bytes `fix_grp`(hi nibble=LOCATION group TYPE, lo nibble=TARGET group TYPE; types 1=Code 2=Data 3=Extra 4=Stack 5-8=Aux) + `fix_para`(2 LE, paragraph of word within location-group IMAGE) + `fix_offs`(1, byte 0..15). Loader: `add es:[loc_load_seg+fix_para : fix_offs], target_load_seg` — a 16-bit ADD, word pre-holds the TARGET's group-relative paragraph (`cpm86GroupImgPara`, NOT wlink frame#). Producer: `obj2supp.c FmtReloc`→`AddCPM86Fixup` (FIX_BASE only), `loadcpm86.c cpm86WriteFixups` (`para=loc_flat>>4`, `rec[3]=loc_flat&0xF`, nibbles=`cpm86GroupCmdType`). **Genuine terminator = byte0==0 ALONE; our Unicorn+emu2 readers test all-4-zero (harmless: real loc-type nibble ≥1).** Trap: header descriptor=9B vs base-page descriptor=6B (len 24-bit BYTES @+0, seg @+3, flag @+5; EXTRA @DS:0x0C — `farheap.c`/Turbo Pascal read it). Status: far GLOBALS now read correctly in compact model (`__heap_enabled`=1); OPEN: far HEAP mis-integrates (farheap.c carves EXTRA offset 0, colliding with program far data now at EXTRA start).
- [emu2-cpm86 implements CP/M-86 P_LOAD relocation (closes #1) — runs wlink medium/compact .CMDs + keeps DR C self-reloc safe](reference_cpm86_emu2_p_load_reloc.md) — 2026-08-20 commit `b5b1a48` (branch `local/cpm86`): loader applies the byte-127-bit-7 + `ch_fixrec` 4-byte fixup table (`add word, target_load_seg`), a port of `load.sup:405-449`. Un-blocks emu2 for pure-loader-reloc output (medium far code, compact type-3 EXTRA). DR C safe (guard-deferral, no double-reloc). Regression: `tests/cpm86-reloc/run.sh` (FARMULTI/FARPTR oracles). Enables all-model disk under emu2.
- [CP/M-86 Watcom clib passes runtime tests in ALL memory models (s/m/c) — compact NO LONGER blocked](reference_cpm86_clib_all_models_gate.md) — 2026-08-20: `watcom-cpm86-libc/run-all-models.sh` is ONE command (builds each model's `clib{s,m,c}.lib` + per-model `libm{s,m,c}.lib` via build-lib.sh if missing, then runs the gate). 21 PASS / 0 FAIL / 0 SKIP: heap/stdio/float/math/fltfmt/scanf under Unicorn, disk under emu2 (emu2 now relocates too). Compact transparent far `malloc()` works (wlink type-3 EXTRA fix `09c2eb3099`). `build-lib.sh` made archive-complete (cprintf, qsort, bexpand, fexpand/fmemcpy, full soft-float pkg; `double` needs `-fpc`). **libm (sin/cos/atan/exp/log/sqrt) is now per-model** — must be, because code model → near/far RET and data model → coefficient tables in DGROUP (s/m) vs embedded in code (c); one libm can't serve all. **Real %e/%f/%g/%a printf + sscanf %f all work** (opt-in: `__setEFGfmt()` once + link libm; %a needs the source-built efgfmt shadowing the prebuilt libm one; `docs/FLOAT_PRINTF.md` + `docs/BUILDING_ALL_MODELS.md`). Also fixed `con_write()` CR/LF cross-call idempotency in `port/diskio.c`.
- [DR C CP/M-86 relocation — VERIFIED guard-coordinated dual path](reference_drc_cpm86_reloc_mechanism_VERIFIED.md) — 2026-08-19: THE authoritative account. A DR C large-model `.CMD` carries the reloc table once; relocation is applied by EXACTLY ONE actor. CLEARL crt0 guard immediate (disasm 0x175, `mov cx,0/jcxz`) is ITSELF a fixup target (RELOCSEG rec #561): loader relocating sets guard nonzero → crt0 skips; non-relocating loader (emu2) leaves guard=0 → crt0 self-relocates via base-page group segments (0x03..0x2d). Never both. Experiments (emu2): RELOCALL→`ABC` (far call reached); guard patched nonzero→CRASH. **emu2 is NOT buggy for DR C.** Self-reloc abort "You must link with LINK86 V1.2 or later" fires when guard=0 AND aux4 group absent (base-page 0x2d=0) — our Unicorn runner hit it. LINK-86 in workspace = v1.4 (19 Mar 1984); byte-fixup load-reloc is Concurrent-era (source dated 29 Jun 1983), undocumented in DRI manuals. Stage B must pick ONE coherent model.
- [CP/M-86 `.CMD` header — AUTHORITATIVE from genuine CCP/M-86 2.0 OS source](reference_cpm86_cmd_header_ccpm_source.md) — 2026-08-19: complete header decode grounded in `scratch/ccpm86-src/kern/{cmdh.def,load.sup,basep.fmt}` (from `ccpm8620.zip`), cross-checked vs real DR C `LL_l/MT_l/LL_s.CMD`. Group descriptor = 9 bytes (`ch_form/length/base/min/max`), 8 max; G_Type 1=Code 2=Data 3=Extra 4=Stack 5-8=Aux 9=Pure/Shared; `ch_base`≠0=absolute. **Byte 127 (`ch_lbyte`): bit 7 (0x80) = fixup records present (loader MAY relocate), bit 6/5 = 8087** — bit 7 IS in OS source `cmdh.def:18` (not the manuals). `ch_fixrec` (word 0x7D) = file RECORD# of the 4-byte fixup table `[grp-nibbles][para:2][byte:1]`; genuine CCP/M loader (`load.sup:405-449`) does `add es:[di],dx`. **But DR C uses a guard-coordinated DUAL path: loader relocates IF it can (sets a relocatable guard nonzero → crt0 skips) ELSE CLEARL crt0 self-relocates (guard=0). Never both.** emu2 is NOT buggy for DR C (guard stays 0, crt0 self-relocs). Full verified account: `[[reference_drc_cpm86_reloc_mechanism_VERIFIED]]`. Base page (`basep.fmt`) built by `load.sup:init_base`; register entry state matches measured RC759. **Newer source now cached:** `[[reference_ccpm86_v31_source]]` (CCP/M-86 v3.1, `scratch/ccpm31-src/`) — 3.1's `LOAD.SUP`+`CMDH.DEF` ENFORCE the 8087 bits (2.0 stubbed them): `need_8087=020H` (bit5/REQUIRED), `opt_8087=040H` (bit6/optional), new `susp_mode=008H` (bit3).
- [Concurrent CP/M-86 v3.1 source — cached (newer than the 2.0 tree)](reference_ccpm86_v31_source.md) — 2026-08-21: newest public CCP/M source (1984), `scratch/ccpm31-src/` (`ccpmv31.zip`, sha256 43d41e0b…, 12 disks D1-D12, 393 files PL/M+ASM) from Tim Olmstead lib `cpm.z80.de/download/ccpmv31.zip`. Strictly newer than v2.0 `scratch/ccpm86-src/`. A web search wrongly claimed nothing >2.0 exists — it does. 3.1 enforces `.CMD` byte-127 8087 bits (bit5 need_8087/REQUIRED, bit6 opt_8087/CONDITIONAL) + new bit3 susp_mode, wiring `pf_8087` and long/emulator UDA — settles what 2.0 left commented-out. Bit-7 fixup mechanism unchanged. Do NOT re-download.
- [Stage B Phase B2 decision RESOLVED 2026-08-19: use real CP/M-86 fixup records (Option 1) — DR C oracle decoded, ruling out fixed load addresses](reference_drc_cpm86_reloc_format.md) — built a clean 2-module DR C 1.11 large-model program under emu2 and decoded its `.CMD`: LINK-86 sets `header[0x7F]=0x80` (byte 127 bit 7 = **fixup records present** — authoritative CCP/M-86 2.0 source `kern/cmdh.def:18`+`kern/load.sup:405`; DRI manuals cover only bit 5/6 = 8087) + a relocation table of 4-byte records `[group-nibbles][para-offset:2 LE][byte-in-para:1]` located via `ch_fixrec` (header word 0x7D = file record #) and also carried as a **type-8 aux group** (low nibble = TARGET group whose load segment is added: 1=CODE,2=DATA); far segment fields are GROUP-RELATIVE paragraphs, **the OS loader adds the real base** (`load.sup` `add es:[di],dx`). **RESOLVED against genuine OS source + DR C disasm: DR C uses a guard-coordinated DUAL path — loader relocates IF it can (sets a relocatable guard immediate nonzero → CLEARL crt0 self-reloc skipped) ELSE crt0 self-relocates (guard=0). Never both; emu2 is NOT buggy for DR C. Verify wlink output under MAME. Full: `[[reference_drc_cpm86_reloc_mechanism_VERIFIED]]`.** ALL descriptors `A_Base=0` → Option 2 (fixed A-Base) RULED OUT. Bonus: DR C large model layout = CODE+DATA+EXTRA(far heap)+STACK, matching Stage A. wlink spec: coalesce CODE→one type-1 descriptor; intercept FORMAT CPM86 segment relocs → fixup records; set byte 127 bit 7 + write ch_fixrec + emit records (+ type-8 aux group for emu2); optionally emit type-4 STACK. Remaining effort MUST boot under MAME, not yet started.
- [Stage B Phase B2 (superseded, was PAUSED mid-decision 2026-08-18 → RESOLVED 2026-08-19, see line above)](../plan-cpm86-big-model-2026-08-18.md) — 2026-08-18, session ended here. Enabled `OPTION PACKCODE` for `format cpm86` (commit `e3ba30be52`, open-watcom-v2) so wlink's existing `AutoGroup` packs same-class CODE segments — works perfectly IF everything stays in one real 64K segment (verified: `-mm -zm` 2-function test, far call auto-relaxed to near). Forcing a genuine 2-real-segment split (tiny PACKCODE, cheap way to test without writing real 64K code) exposed TWO problems: (1) `loadcpm86.c` emits one G-Type=1 descriptor PER wlink group (needs merging — independent sub-task, do regardless of the fixup decision), (2) the far `jmp`'s encoded segment operand came out as `0000` instead of the real value, silently, no warning — because CP/M-86 `.CMD` groups are each independently loader-relocated (`A-Base=0`) but wlink's generic fixup engine assumes a DOS-like single link-time-known base. The three-way decision this left open is now settled — see `reference_drc_cpm86_reloc_format.md` (Option 1, real fixup records).
- [Stage B (code>64K) planning: `-mm -zm` already gives per-function far-callable CODE segments — zero new compiler code needed](../plan-cpm86-big-model-2026-08-18.md) — 2026-08-18. DR C oracle (wdis -a on `.obj`, not the linked `.CMD`) confirmed its big model's exact contract: each compiled MODULE gets a uniquely-named `CODE`-class segment + unconditional `call far ptr` to externs. Watcom-side agent audit (file:line cited in `reference_cpm86_big_model.md`) found `-mm` ALONE does nothing for segment naming (still `_TEXT`, model-independent) — but `-mm -zm` together (existing Watcom overlay/smart-link mechanism, `cdecl1.c:182-201`) already gives one segment PER FUNCTION (finer than DR C's per-file), verified empirically (`wdis -a` on a `wcc -mm -zm` test object: `callee_TEXT`/`main_TEXT`, `jmp far ptr callee_`). wlink's own group-formation is also model-independent/opt-in-only (`-g` switch) — confirms Stage B is a PURE wlink change (Phase B2: pack many same-class CODE segments into one Code Group Descriptor), no compiler work. Naming question (parked) now has a candidate answer: this is exactly Watcom's own "medium model" (`-mm`: far code/near data), not large.
- [Stage A far heap (wlink OPTION FARHEAP + port/farheap.c multi-slab __AllocSeg, G_Min/G_Max range) — emu2 AND real genuine-CCP/M-86 MAME rc759 verified](reference_cpm86_big_model.md) — 2026-08-18, commits `4cd6304d3a`+`b8cfd0d10e`+`d9b1de395b`+`3c79eea897`+`2d809fc6da`: wlink emits a type-3 Extra descriptor, `G_Min=1` paragraph / `G_Max=<FARHEAP size>` (NOT min==max — a min==max==300K request got outright REFUSED to load by real Concurrent CP/M-86, "Concurrent Fejl: For lidt lager"; min=1/max=ceiling lets the loader grant whatever's actually free). `port/farheap.c`'s `__AllocSeg` reads the REAL grant off the base page (not a compiled-in constant) and carves successive ≤64K slabs out of it. `test/farheaptest.c` loops `_fmalloc` until exhaustion (not a fixed block count) with an overlap-detecting fill pattern — per-block START **and SLOPE** both vary (two different periods, 256 and 251) after a review catch: a single shared slope made blocks 256 apart in index get an IDENTICAL sequence (undetectable overlap), a real bug once run-to-exhaustion routinely produces 250+ blocks. Verified emu2 (277 blocks/637,352 B) AND real MAME rc759 hardware on the GENUINE CCP/M-86 disk (not CDOS — see next line): 79 blocks/181,647 B, 0 corrupted, out of RC759's real 261K brugerlager. Plan: `tasks/plan-cpm86-big-model-2026-08-18.md` Phase A2/A4.
- [MAME rc759: genuine CCP/M-86 disk (sw1400-r3.1a) already boots as a 4-console system; `0:startup.0` is a plain-text auto-command file you can override](reference_ccpm86_boot_disk_and_4console_todo.md) — 2026-08-18 correction: the old "needs installing" 4-console TODO was WRONG — `scratch/rc759-cmd-toolchain/ddhf-cache/derived/sw1400-r3.1a-disk1.img` prints "System med 4 konsoller" at every boot, it's already a ready system. It auto-starts `menu imenu` via `0:startup.0` (a plain 128-byte CP/M text record) instead of dropping to `A>` — overwrite that file (cpmtools `cpmrm`+`cpmcp` on a FRESH disk copy) with any command (e.g. `b:prog`) to boot straight into it, far more reliable than timing `natkeyboard` through the interactive installer menu's nested submenus/confirmation prompts (`ESC` then `J`=ja). Also: MAME rc759 RAM is hardcoded 384K in driver source, not configurable; boot banner shows both total (384K) and free-for-programs (261K "brugerlager") RAM.
- [MAME rc759 A: boot-disk pitfall: Bits:30002664 does NOT boot ("DISKETTE NOT FORMATTED"); Bits:30002654 is CDOS not CCP/M-86 despite booting fine — use the genuine CCP/M-86 disk above](reference_rc759_mame_sonnyboy_headless.md) — re-hit 2026-08-18 despite already being documented (see `feedback_check_memory_before_coding`'s incident log) — Bits:30002664 "DR C May 84" is an application/toolkit disk, not bootable; Bits:30002654 "CDOS systemdisk" boots fine and even prints "Concurrent Fejl"-style errors (CDOS shares CCP/M-86's BDOS error text) but is a DIFFERENT DRI product, not genuine CCP/M-86 — use `sw1400-r3.1a-disk1.img` (line above) when the ask is specifically CCP/M-86.
- [Stage B far-code relocation VERIFIED — image-paragraph layout bug fixed + base-page reservation trap + two runtime oracles](reference_stageb_farcode_reloc_verified.md) — 2026-08-19: wlink `-mm -zm` CP/M-86 load-time relocation implemented and RUNTIME-verified on Unicorn. (1) THE linker bug: a far target's group-relative paragraph MUST come from the packed `.CMD` image layout (`cpm86GroupImgPara` = running sum of `CMD_PARAS(CalcGroupSize())`), NOT `grp_addr.seg` — wlink frame numbers increment by 1 per segment regardless of size, but images are paragraph-packed (correct {5,6,7,9} vs buggy {1,2,3,4}). (2) Base-page trap (test-side): genuine CCP/M-86 `load.sup:477 init_base` zero-fills the start of the first DATA group, clobbering DS:0000 data — freestanding tests must link a BEGDATA reservation module first (`test_stageb_begdata.asm`); real programs get it from `cstartcpm.asm`. (3) Two committed runtime oracles in `contrib/ravn/test_stageb_farcall.sh`: CALL (CODE→CODE, fixup nibble 0x1X) + POINTER (DATA→CODE far pointer MEMORY check, nibble 0x2X — the technique to reuse for large programs like UnZip) + small-model no-fixup guard. All pass. Still TODO: MAME verify, medium-model clib/crt0, UnZip port.
- [Info-ZIP `zip` på CP/M-86 kræver LARGE model (ikke medium)](reference_zip_cpm86_needs_large_model.md) — 2026-08-21. zip.h prototyper flush_block `char far*` men trees.c-def er `char*` → E1129 i alle near-data-modeller (small+medium). Kilden er pristine (deles med DOS-build), må ikke edites. Large (far code+far data) gør char*==char far* → alle 11 kerne-objekter oversætter rent, nul edits; far code spænder flere frames. Kontrast: unzip klarede small model. 2026-08-25: deflate-only CCP/M-vs-emu2 divergence remains open; STORE passes both.
- [CP/M-86 FCB-wildcard: BDOS search matcher kun '?', ikke '*'](reference_cpm86_fcb_wildcard_qmark.md) — 2026-08-21. BDOS Search First/Next (17/18) matcher '?' pr. tegnposition i FCB; '*' er kun CCP-bekvemmelighed der ekspanderer til '?'-padding. zip's wild() må enten selv konvertere *→? før FCB, eller (unzip-precedent) droppe globbing og tage argumenter literalt.
- [Vis løbende fremdrift på lange kørsler](feedback_show_progress_on_long_runs.md) — 2026-08-21. Læg en Monitor der streamer fasemarkører live (bygger model X, PASS/FAIL, matrix) på enhver kørsel >~30s; tavs baggrundskørsel er ikke til at skelne fra en hængt/racende.
- [PR branch edits back to working branch](feedback_pr_branch_edits_back_to_working.md) — when rebuilding PR branches with cleaned-up commits (shorter comments, removed noise), also apply those edits back to the working branch (local/cpm86 etc.) — otherwise the two diverge.
- [CCP/M-86 folder kommando-halen til UPPERCASE (MAME-verificeret)](reference_ccpm86_uppercases_command_tail.md) — 2026-08-25. Rigtig RC759 CCP/M-86 folder command-tail til uppercase (tastede `taildump abcXYZ` småt → program fik `[ ABCXYZ]`/`argv1=[ABCXYZ]`), selvom konsollen ekkoer småt. Dok §6.2.7 dokumenterer kun F_PARSE-foldning af FCB-felter, ikke 0x80-halen — MAME afgjorde det. Intet BDOS-kald giver den pre-foldede linje (CLBUF er forælderens; base-page er foldet). Konsekvens: zips case-distinkte options (`-d`/`-D` …) kolliderer; håndter via lowercase-map i argv + case-insensitive lange optioner. emu2 er IKKE sandhedsvidne for CCP/M-adfærd, kun MAME.
